/* Django — Python web framework (interview-prep) */
PREP.register({
  id: "django",
  icon: "🐍",
  category: "backend",
  title: { vi: "Django (Python web framework)", en: "Django (Python web framework)" },
  blurb: {
    vi: "Framework Python \"batteries-included\" theo mô hình MTV. Ôn chắc ORM (QuerySet lazy, select_related vs prefetch_related, N+1), request lifecycle, CBV vs FBV, middleware, bảo mật tích hợp, DRF, và WSGI/ASGI là đủ cho hầu hết câu hỏi backend.",
    en: "A \"batteries-included\" Python framework built on the MTV pattern. Master the ORM (lazy QuerySets, select_related vs prefetch_related, N+1), the request lifecycle, CBV vs FBV, middleware, built-in security, DRF, and WSGI/ASGI to cover most backend questions.",
  },
  sections: [
    {
      id: "what-is",
      title: { vi: "1. Django là gì & mô hình MTV", en: "1. What Django is & the MTV pattern" },
      blocks: [
        { type: "prose", vi: "<b>Django</b> là một <b>high-level web framework</b> viết bằng Python, triết lý <b>\"batteries-included\"</b> (mọi thứ kèm sẵn: ORM, admin, auth, forms, template, migrations) và <b>convention over configuration</b> (ưu tiên quy ước hơn cấu hình). Mục tiêu: viết web nhanh, sạch, an toàn — \"the web framework for perfectionists with deadlines\".", en: "<b>Django</b> is a <b>high-level web framework</b> written in Python, with a <b>\"batteries-included\"</b> philosophy (ORM, admin, auth, forms, templates, migrations all bundled) and <b>convention over configuration</b>. Goal: build web apps fast, cleanly, and securely — \"the web framework for perfectionists with deadlines\"." },
        { type: "prose", vi: "Django gọi kiến trúc của mình là <b>MTV (Model–Template–View)</b>, thực chất là một biến thể của <b>MVC</b>. Điểm dễ gây nhầm: phần \"Controller\" của MVC chính là <b>framework Django</b> tự lo (định tuyến URL → gọi view), còn cái Django gọi là \"View\" tương ứng với \"Controller\" trong MVC kinh điển.", en: "Django calls its architecture <b>MTV (Model–Template–View)</b>, essentially a variant of <b>MVC</b>. The common gotcha: the MVC \"Controller\" is handled by the <b>Django framework itself</b> (URL routing → calling the view), while what Django calls the \"View\" maps to the \"Controller\" in classic MVC." },
        { type: "table",
          headers: { vi: ["Django (MTV)", "Vai trò", "Tương ứng MVC"], en: ["Django (MTV)", "Role", "MVC equivalent"] },
          rows: [
            { vi: ["Model", "Dữ liệu & business logic, ánh xạ tới bảng DB qua ORM", "Model"], en: ["Model", "Data & business logic, mapped to DB tables via the ORM", "Model"] },
            { vi: ["Template", "Lớp trình bày (HTML), hiển thị dữ liệu", "View"], en: ["Template", "Presentation layer (HTML), renders data", "View"] },
            { vi: ["View", "Nhận request, xử lý logic, trả response", "Controller"], en: ["View", "Receives request, runs logic, returns response", "Controller"] },
            { vi: ["(URL dispatcher)", "Định tuyến URL → view; do framework lo", "Controller (phần routing)"], en: ["(URL dispatcher)", "Routes URL → view; handled by framework", "Controller (routing part)"] },
          ] },
        { type: "callout", variant: "key", vi: "Nhớ một câu: <b>MTV = MVC nhưng Django gọi tên khác</b>. Template = View của MVC, View của Django = Controller của MVC, còn \"Controller\" thật là chính framework (URL dispatcher).", en: "One-liner: <b>MTV = MVC with Django's own names</b>. Template = MVC's View, Django's View = MVC's Controller, and the real \"Controller\" is the framework itself (the URL dispatcher)." },
      ],
    },
    {
      id: "core",
      title: { vi: "2. Các thành phần cốt lõi", en: "2. Core components" },
      blocks: [
        { type: "list", items: [
          { vi: "<b>ORM (Object-Relational Mapper)</b> — định nghĩa bảng DB bằng class Python (model). Truy vấn qua <b>QuerySet</b> thay vì viết SQL tay.", en: "<b>ORM (Object-Relational Mapper)</b> — define DB tables as Python classes (models). Query via <b>QuerySets</b> instead of hand-writing SQL." },
          { vi: "<b>Migrations</b> — phiên bản hóa thay đổi schema. <code>makemigrations</code> sinh file migration từ thay đổi model; <code>migrate</code> áp dụng vào DB.", en: "<b>Migrations</b> — version-control schema changes. <code>makemigrations</code> generates migration files from model changes; <code>migrate</code> applies them to the DB." },
          { vi: "<b>URL routing (urls.py)</b> — ánh xạ URL pattern → view bằng <code>path()</code> / <code>re_path()</code>. Hỗ trợ <code>include()</code> để chia route theo app.", en: "<b>URL routing (urls.py)</b> — map URL patterns → views with <code>path()</code> / <code>re_path()</code>. Use <code>include()</code> to split routes per app." },
          { vi: "<b>Views</b> — hàm hoặc class nhận <code>request</code>, trả <code>HttpResponse</code>. Có <b>function-based (FBV)</b> và <b>class-based (CBV)</b>.", en: "<b>Views</b> — functions or classes that take a <code>request</code> and return an <code>HttpResponse</code>. Both <b>function-based (FBV)</b> and <b>class-based (CBV)</b>." },
          { vi: "<b>Templates</b> — Django Template Language (DTL): <code>{{ var }}</code>, <code>{% tag %}</code>, filter. <b>Tự động escape HTML</b> chống XSS.", en: "<b>Templates</b> — Django Template Language (DTL): <code>{{ var }}</code>, <code>{% tag %}</code>, filters. <b>Auto-escapes HTML</b> to prevent XSS." },
          { vi: "<b>Admin site</b> — giao diện quản trị CRUD sinh tự động từ model; chỉ cần <code>register</code>. Lợi thế lớn nhất của \"batteries-included\".", en: "<b>Admin site</b> — an auto-generated CRUD admin UI from your models; just <code>register</code> them. The flagship \"batteries-included\" feature." },
          { vi: "<b>Forms</b> — định nghĩa, render và <b>validate</b> dữ liệu nhập. <code>ModelForm</code> sinh form trực tiếp từ model.", en: "<b>Forms</b> — define, render and <b>validate</b> user input. <code>ModelForm</code> builds a form straight from a model." },
          { vi: "<b>Middleware</b> — các lớp xử lý request/response theo kiểu \"hành tây\" bao quanh view (auth, CSRF, session, gzip…).", en: "<b>Middleware</b> — layers that wrap the view like an \"onion\", processing request/response (auth, CSRF, sessions, gzip…)." },
        ] },
        { type: "callout", variant: "tip", vi: "Một project Django gồm nhiều <b>app</b> (module tái sử dụng được). <code>django-admin startproject</code> tạo project; <code>startapp</code> tạo app. Đăng ký app trong <code>INSTALLED_APPS</code> ở <code>settings.py</code>.", en: "A Django project is made of several <b>apps</b> (reusable modules). <code>django-admin startproject</code> creates a project; <code>startapp</code> creates an app. Register apps in <code>INSTALLED_APPS</code> in <code>settings.py</code>." },
      ],
    },
    {
      id: "lifecycle",
      title: { vi: "3. Vòng đời request/response", en: "3. Request/response lifecycle" },
      blocks: [
        { type: "prose", vi: "Khi một request đến, Django đi qua các bước sau. Hiểu rõ thứ tự này giúp trả lời câu \"middleware chạy lúc nào?\" và \"CSRF kiểm tra ở đâu?\".", en: "When a request arrives, Django walks through these steps. Knowing the order answers \"when does middleware run?\" and \"where is CSRF checked?\"." },
        { type: "list", ordered: true, items: [
          { vi: "<b>WSGI/ASGI server</b> (Gunicorn/uvicorn) nhận request HTTP và chuyển cho Django.", en: "The <b>WSGI/ASGI server</b> (Gunicorn/uvicorn) receives the HTTP request and hands it to Django." },
          { vi: "<b>Middleware (chiều request)</b> chạy lần lượt từ trên xuống (security, session, auth, CSRF…).", en: "<b>Middleware (request phase)</b> runs top-to-bottom (security, session, auth, CSRF…)." },
          { vi: "<b>URL dispatcher</b> so khớp URL với <code>urlpatterns</code> để chọn view.", en: "The <b>URL dispatcher</b> matches the URL against <code>urlpatterns</code> to select a view." },
          { vi: "<b>View</b> chạy: truy vấn ORM, xử lý logic, dựng response (render template hoặc trả JSON).", en: "The <b>view</b> runs: queries the ORM, runs logic, builds a response (render a template or return JSON)." },
          { vi: "<b>Middleware (chiều response)</b> chạy ngược lại từ dưới lên trước khi trả về client.", en: "<b>Middleware (response phase)</b> runs in reverse, bottom-to-top, before returning to the client." },
        ] },
        { type: "callout", variant: "warning", vi: "Middleware là kiểu <b>hành tây (onion)</b>: chiều request đi xuôi danh sách, chiều response đi ngược. Đặt sai thứ tự middleware (vd CSRF, auth) có thể gây lỗi bảo mật hoặc 403.", en: "Middleware is an <b>onion</b>: request goes down the list, response comes back up. Misordering middleware (e.g. CSRF, auth) can cause security bugs or 403s." },
      ],
    },
    {
      id: "orm",
      title: { vi: "4. ORM — điểm hay bị hỏi sâu", en: "4. The ORM — common deep-dive questions" },
      blocks: [
        { type: "prose", vi: "ORM là phần được hỏi sâu nhất. Hai chủ đề kinh điển: <b>QuerySet lazy</b> và bài toán <b>N+1 query</b> (cùng cách fix bằng <code>select_related</code> / <code>prefetch_related</code>).", en: "The ORM is the most deeply probed area. Two classic topics: <b>lazy QuerySets</b> and the <b>N+1 query</b> problem (and fixing it with <code>select_related</code> / <code>prefetch_related</code>)." },
        { type: "code", code: "from django.db import models\n\nclass Author(models.Model):\n    name = models.CharField(max_length=100)\n\nclass Book(models.Model):\n    title = models.CharField(max_length=200)\n    author = models.ForeignKey(Author, on_delete=models.CASCADE)\n\n# LAZY: khong cham DB cho toi khi duyet / list()\nqs = Book.objects.filter(title__icontains=\"django\")\n\n# N+1 PROBLEM: 1 query lay books, roi MOI book + 1 query lay author\nfor b in Book.objects.all():\n    print(b.author.name)   # +1 query moi vong lap!\n\n# FIX: select_related dung SQL JOIN -> chi 1 query\nfor b in Book.objects.select_related(\"author\"):\n    print(b.author.name)   # author da nam san trong cache", caption: { vi: "Model đơn giản + bẫy N+1 và cách fix bằng <code>select_related</code> (Python).", en: "A simple model + the N+1 trap and the <code>select_related</code> fix (Python)." } },
        { type: "list", items: [
          { vi: "<b>QuerySet lazy (lười)</b> — tạo/filter QuerySet <b>không</b> chạm DB ngay. SQL chỉ chạy khi bạn <b>evaluate</b> nó: lặp <code>for</code>, <code>list()</code>, slicing, <code>len()</code>, <code>bool()</code>, hoặc in ra. Cho phép xích nhiều filter mà chỉ tốn 1 query cuối.", en: "<b>Lazy QuerySets</b> — building/filtering a QuerySet does <b>not</b> hit the DB. SQL runs only when you <b>evaluate</b> it: iterate with <code>for</code>, <code>list()</code>, slicing, <code>len()</code>, <code>bool()</code>, or print it. Lets you chain many filters with a single final query." },
          { vi: "<b>N+1 query problem</b> — lấy N object rồi truy cập quan hệ của từng cái → tốn 1 + N query. Triệu chứng kinh điển của hiệu năng kém.", en: "<b>N+1 query problem</b> — fetch N objects then access each one's relation → 1 + N queries. The classic performance smell." },
          { vi: "<b>select_related</b> — dùng <b>SQL JOIN</b>, gom dữ liệu liên quan vào <b>cùng một query</b>. Chỉ cho quan hệ \"đơn\": <b>ForeignKey</b> và <b>OneToOne</b> (phía \"forward\").", en: "<b>select_related</b> — uses a <b>SQL JOIN</b> to pull related data in <b>one query</b>. For \"single-valued\" relations: <b>ForeignKey</b> and <b>OneToOne</b> (forward side)." },
          { vi: "<b>prefetch_related</b> — chạy <b>query riêng</b> cho mỗi quan hệ rồi ghép trong Python. Dùng cho <b>ManyToMany</b> và <b>reverse ForeignKey</b> (quan hệ \"nhiều\").", en: "<b>prefetch_related</b> — runs a <b>separate query</b> per relation and joins them in Python. For <b>ManyToMany</b> and <b>reverse ForeignKey</b> (\"multi-valued\" relations)." },
          { vi: "<b>F() object</b> — tham chiếu giá trị một cột ngay trong DB, tránh đọc về Python (an toàn race-condition): <code>F(\"views\") + 1</code>.", en: "<b>F() objects</b> — reference a column's value directly in the DB, avoiding a round-trip to Python (race-safe): <code>F(\"views\") + 1</code>." },
          { vi: "<b>Q() object</b> — ghép điều kiện phức tạp với <b>OR / AND / NOT</b>: <code>Q(a=1) | Q(b=2)</code>. Filter thường chỉ là AND.", en: "<b>Q() objects</b> — build complex conditions with <b>OR / AND / NOT</b>: <code>Q(a=1) | Q(b=2)</code>. Plain filters are AND-only." },
          { vi: "<b>Transactions</b> — <code>transaction.atomic()</code> (decorator hoặc context manager) gói nhiều thao tác thành một đơn vị: thành công thì commit, lỗi thì rollback toàn bộ.", en: "<b>Transactions</b> — <code>transaction.atomic()</code> (decorator or context manager) wraps operations into one unit: commit on success, roll back everything on error." },
        ] },
        { type: "callout", variant: "soundbite", vi: "Câu chốt: <b>\"select_related dùng JOIN, một query, cho ForeignKey/OneToOne; prefetch_related chạy query riêng rồi ghép trong Python, cho ManyToMany/reverse FK.\"</b> Cả hai đều để giết N+1.", en: "Soundbite: <b>\"select_related uses a JOIN, one query, for ForeignKey/OneToOne; prefetch_related runs a separate query and joins in Python, for ManyToMany/reverse FK.\"</b> Both kill N+1." },
        { type: "callout", variant: "danger", vi: "Đừng gọi <code>.count()</code> rồi lại <code>len(list(qs))</code> trên cùng QuerySet — đó là 2 query. Và tránh <code>qs[i]</code> trong vòng lặp. Dùng <code>django-debug-toolbar</code> để soi số query thực tế.", en: "Don't call <code>.count()</code> then <code>len(list(qs))</code> on the same QuerySet — that's 2 queries. Avoid <code>qs[i]</code> inside loops. Use <code>django-debug-toolbar</code> to inspect the real query count." },
      ],
    },
    {
      id: "views",
      title: { vi: "5. Views: FBV vs CBV & forms", en: "5. Views: FBV vs CBV & forms" },
      blocks: [
        { type: "table",
          headers: { vi: ["Tiêu chí", "Function-Based View (FBV)", "Class-Based View (CBV)"], en: ["Aspect", "Function-Based View (FBV)", "Class-Based View (CBV)"] },
          rows: [
            { vi: ["Cú pháp", "Một hàm nhận request", "Một class kế thừa View / generic"], en: ["Syntax", "A function taking request", "A class extending View / generics"] },
            { vi: ["Dễ đọc", "Rõ ràng, tường minh, dễ với bài đơn giản", "Trừu tượng hơn, cần biết MRO/mixin"], en: ["Readability", "Explicit, easy for simple cases", "More abstract, need MRO/mixins knowledge"] },
            { vi: ["Tái sử dụng", "Lặp code cho CRUD", "Generic views (ListView, DetailView…) giảm boilerplate"], en: ["Reuse", "Repetitive for CRUD", "Generic views (ListView, DetailView…) cut boilerplate"] },
            { vi: ["Mở rộng", "Dùng decorator", "Dùng mixin & kế thừa"], en: ["Extensibility", "Via decorators", "Via mixins & inheritance"] },
          ] },
        { type: "list", items: [
          { vi: "<b>Forms / ModelForm</b> — gọi <code>form.is_valid()</code> chạy validation; dữ liệu sạch nằm trong <code>form.cleaned_data</code>. <code>ModelForm</code> map thẳng tới model và có <code>.save()</code>.", en: "<b>Forms / ModelForm</b> — <code>form.is_valid()</code> runs validation; clean data lives in <code>form.cleaned_data</code>. <code>ModelForm</code> maps directly to a model and offers <code>.save()</code>." },
          { vi: "<b>Signals</b> — cơ chế pub/sub: <code>pre_save</code>, <code>post_save</code>, <code>post_delete</code>… cho phép tách logic phụ (vd tạo profile khi tạo user). Dùng vừa phải vì khó debug.", en: "<b>Signals</b> — a pub/sub mechanism: <code>pre_save</code>, <code>post_save</code>, <code>post_delete</code>… to decouple side effects (e.g. create a profile when a user is created). Use sparingly — they're hard to trace." },
        ] },
        { type: "callout", variant: "tip", vi: "Quy tắc thực dụng: dùng <b>CBV/generic views</b> cho CRUD tiêu chuẩn (đỡ boilerplate), dùng <b>FBV</b> khi logic đặc thù, ít lặp, cần đọc rõ ràng.", en: "Pragmatic rule: use <b>CBV/generic views</b> for standard CRUD (less boilerplate), use <b>FBV</b> for bespoke, one-off logic where clarity matters." },
      ],
    },
    {
      id: "drf",
      title: { vi: "6. Django REST Framework (DRF)", en: "6. Django REST Framework (DRF)" },
      blocks: [
        { type: "prose", vi: "Django thuần render HTML; để xây <b>API JSON</b>, dùng <b>Django REST Framework (DRF)</b> — thư viện chuẩn de-facto. Ba khái niệm cốt lõi:", en: "Plain Django renders HTML; to build <b>JSON APIs</b> you use <b>Django REST Framework (DRF)</b> — the de-facto standard library. Three core concepts:" },
        { type: "list", items: [
          { vi: "<b>Serializer</b> — chuyển model/QuerySet ↔ JSON (serialize) và <b>validate</b> dữ liệu vào (deserialize). <code>ModelSerializer</code> sinh field tự động từ model — vai trò giống Form cho API.", en: "<b>Serializer</b> — converts models/QuerySets ↔ JSON (serialize) and <b>validates</b> incoming data (deserialize). <code>ModelSerializer</code> auto-generates fields from a model — the API analogue of a Form." },
          { vi: "<b>ViewSet</b> — gom logic CRUD cho một resource vào một class (list/retrieve/create/update/destroy). <code>ModelViewSet</code> cho full CRUD gần như không cần viết tay.", en: "<b>ViewSet</b> — groups a resource's CRUD logic into one class (list/retrieve/create/update/destroy). <code>ModelViewSet</code> gives full CRUD almost for free." },
          { vi: "<b>Router</b> — tự sinh URL từ ViewSet (<code>DefaultRouter</code>), khỏi viết <code>urlpatterns</code> thủ công cho mỗi endpoint.", en: "<b>Router</b> — auto-generates URLs from a ViewSet (<code>DefaultRouter</code>), so you don't hand-write <code>urlpatterns</code> per endpoint." },
        ] },
        { type: "callout", variant: "key", vi: "Mạch tư duy DRF: <b>Serializer</b> (validate + JSON) → <b>ViewSet</b> (CRUD logic) → <b>Router</b> (URL). Đây là cách Django dựng RESTful API nhanh.", en: "DRF mental model: <b>Serializer</b> (validate + JSON) → <b>ViewSet</b> (CRUD logic) → <b>Router</b> (URLs). This is how Django builds RESTful APIs fast." },
      ],
    },
    {
      id: "security",
      title: { vi: "7. Bảo mật \"miễn phí\"", en: "7. Built-in security \"for free\"" },
      blocks: [
        { type: "prose", vi: "Django được hỏi nhiều về bảo mật vì nó bật sẵn nhiều lớp phòng vệ. Cần kể được các cơ chế này:", en: "Security comes up a lot because Django ships many protections enabled by default. You should be able to list these:" },
        { type: "table",
          headers: { vi: ["Mối đe dọa", "Django chặn thế nào"], en: ["Threat", "How Django defends"] },
          rows: [
            { vi: ["CSRF", "Middleware + token <code>{% csrf_token %}</code> bắt buộc cho POST form"], en: ["CSRF", "Middleware + a <code>{% csrf_token %}</code> required on POST forms"] },
            { vi: ["SQL Injection", "ORM tham số hóa (parameterize) mọi truy vấn → không nối chuỗi SQL"], en: ["SQL Injection", "The ORM parameterizes every query → no string-concatenated SQL"] },
            { vi: ["XSS", "Template tự động escape HTML trong <code>{{ var }}</code>"], en: ["XSS", "Templates auto-escape HTML inside <code>{{ var }}</code>"] },
            { vi: ["Clickjacking", "<code>X-Frame-Options</code> qua middleware"], en: ["Clickjacking", "<code>X-Frame-Options</code> via middleware"] },
            { vi: ["Mật khẩu yếu/lộ", "Hash mật khẩu bằng PBKDF2 (mặc định) + có salt, không lưu plaintext"], en: ["Weak/leaked passwords", "Passwords hashed with PBKDF2 (default) + salt, never stored in plaintext"] },
          ] },
        { type: "list", items: [
          { vi: "<b>Hệ thống auth</b> — User model, login/logout, permission & group, decorator <code>@login_required</code>, mixin <code>LoginRequiredMixin</code>.", en: "<b>Auth system</b> — a User model, login/logout, permissions & groups, the <code>@login_required</code> decorator, the <code>LoginRequiredMixin</code>." },
          { vi: "<b>Cảnh báo XSS</b> — nếu bạn dùng <code>|safe</code> hoặc <code>mark_safe()</code> là tự tắt escape → phải tự đảm bảo dữ liệu an toàn.", en: "<b>XSS caveat</b> — using <code>|safe</code> or <code>mark_safe()</code> turns off escaping → you must ensure the data is safe yourself." },
        ] },
        { type: "callout", variant: "warning", vi: "Lỗ hổng thường do <b>lập trình viên tự vô hiệu hóa</b> phòng vệ: dùng <code>.raw()</code>/<code>extra()</code> với chuỗi nối tay (SQLi), hoặc <code>|safe</code> bừa bãi (XSS). Mặc định Django đã an toàn — đừng tắt nó.", en: "Most holes come from <b>developers disabling</b> the defenses: <code>.raw()</code>/<code>extra()</code> with hand-built strings (SQLi), or sprinkling <code>|safe</code> (XSS). Django is safe by default — don't switch it off." },
      ],
    },
    {
      id: "wsgi-asgi",
      title: { vi: "8. WSGI vs ASGI & async", en: "8. WSGI vs ASGI & async" },
      blocks: [
        { type: "prose", vi: "Django chạy sau một server tuân chuẩn giao tiếp Python-web. Hai chuẩn: <b>WSGI</b> (đồng bộ, truyền thống) và <b>ASGI</b> (bất đồng bộ, hiện đại).", en: "Django runs behind a server speaking a Python-web standard. Two standards: <b>WSGI</b> (synchronous, traditional) and <b>ASGI</b> (asynchronous, modern)." },
        { type: "list", items: [
          { vi: "<b>WSGI</b> (Web Server Gateway Interface) — <b>đồng bộ</b>, một request một worker tới khi xong. Server: Gunicorn, uWSGI. Phù hợp app request/response thuần.", en: "<b>WSGI</b> (Web Server Gateway Interface) — <b>synchronous</b>, one request per worker until done. Servers: Gunicorn, uWSGI. Fine for plain request/response apps." },
          { vi: "<b>ASGI</b> (Asynchronous Server Gateway Interface) — <b>bất đồng bộ</b>, hỗ trợ <code>async def</code> view, WebSocket, long-polling. Server: uvicorn, Daphne. Cần cho real-time.", en: "<b>ASGI</b> (Asynchronous Server Gateway Interface) — <b>asynchronous</b>, supports <code>async def</code> views, WebSockets, long-polling. Servers: uvicorn, Daphne. Needed for real-time." },
          { vi: "<b>Async views</b> — từ Django 3.1+ có thể viết <code>async def view(request)</code> cho I/O-bound (gọi API ngoài). Lưu ý: ORM còn chủ yếu đồng bộ — async views không tự làm DB nhanh hơn.", en: "<b>Async views</b> — since Django 3.1+ you can write <code>async def view(request)</code> for I/O-bound work (external API calls). Note: the ORM is still largely synchronous — async views don't automatically speed up DB work." },
          { vi: "<b>Django Channels</b> — thư viện thêm hỗ trợ WebSocket/real-time trên nền ASGI (chat, notification).", en: "<b>Django Channels</b> — a library adding WebSocket/real-time support on top of ASGI (chat, notifications)." },
        ] },
        { type: "callout", variant: "tip", vi: "Một câu phân biệt: <b>WSGI = đồng bộ (request/response cổ điển), ASGI = bất đồng bộ (async + WebSocket)</b>. Phần lớn app CRUD vẫn chạy WSGI tốt; chọn ASGI khi cần real-time hoặc nhiều I/O chờ.", en: "One-liner: <b>WSGI = synchronous (classic request/response), ASGI = asynchronous (async + WebSockets)</b>. Most CRUD apps run fine on WSGI; pick ASGI when you need real-time or lots of waiting I/O." },
      ],
    },
    {
      id: "compare",
      title: { vi: "9. Django vs Flask vs FastAPI", en: "9. Django vs Flask vs FastAPI" },
      blocks: [
        { type: "table",
          headers: { vi: ["Tiêu chí", "Django", "Flask", "FastAPI"], en: ["Aspect", "Django", "Flask", "FastAPI"] },
          rows: [
            { vi: ["Triết lý", "Batteries-included, monolith", "Micro-framework, tối giản", "Async API hiện đại"], en: ["Philosophy", "Batteries-included monolith", "Micro-framework, minimal", "Modern async API"] },
            { vi: ["ORM", "Có sẵn (Django ORM)", "Không (tự chọn SQLAlchemy)", "Không (thường dùng SQLAlchemy)"], en: ["ORM", "Built-in (Django ORM)", "None (bring SQLAlchemy)", "None (usually SQLAlchemy)"] },
            { vi: ["Admin", "Có sẵn, tự sinh", "Không", "Không"], en: ["Admin", "Built-in, auto-generated", "None", "None"] },
            { vi: ["Async", "Hỗ trợ (ASGI, async views)", "Hạn chế", "Async-first, rất mạnh"], en: ["Async", "Supported (ASGI, async views)", "Limited", "Async-first, excellent"] },
            { vi: ["Validation/Docs", "Forms/DRF", "Tự thêm", "Pydantic + OpenAPI tự sinh"], en: ["Validation/Docs", "Forms/DRF", "Add yourself", "Pydantic + auto OpenAPI docs"] },
            { vi: ["Hợp với", "App lớn full-feature, có admin/DB", "App nhỏ, linh hoạt, tự lắp ráp", "API hiệu năng cao, microservice"], en: ["Best for", "Large full-feature apps, admin/DB", "Small, flexible, assemble-it-yourself apps", "High-performance APIs, microservices"] },
          ] },
        { type: "callout", variant: "soundbite", vi: "Tóm: <b>Django</b> cho sẵn mọi thứ (đổi lấy nặng & ý kiến mạnh); <b>Flask</b> tối giản, bạn tự lắp; <b>FastAPI</b> sinh sau, async-first + Pydantic + docs tự động cho API. Chọn theo việc cần \"đủ đồ\" hay \"nhẹ & linh hoạt\".", en: "In short: <b>Django</b> gives you everything (at the cost of weight & strong opinions); <b>Flask</b> is minimal, you assemble it; <b>FastAPI</b> is newer, async-first with Pydantic + auto docs for APIs. Choose by whether you need \"everything included\" or \"light & flexible\"." },
      ],
    },
    {
      id: "when",
      title: { vi: "10. Khi nào dùng Django — ưu/nhược", en: "10. When to use Django — pros & cons" },
      blocks: [
        { type: "list", items: [
          { vi: "<b>Ưu:</b> phát triển nhanh nhờ batteries-included; admin tự sinh; ORM mạnh; bảo mật mặc định tốt; cộng đồng & tài liệu lớn; ổn định, ít \"surprise\".", en: "<b>Pros:</b> fast development thanks to batteries-included; auto admin; powerful ORM; strong default security; huge community & docs; stable, few surprises." },
          { vi: "<b>Nhược:</b> nặng & nhiều quy ước (overkill cho service nhỏ); ORM/monolith khó tùy biến tận gốc; async là \"bổ sung sau\" nên chưa mượt như FastAPI; dễ vướng \"the Django way\".", en: "<b>Cons:</b> heavy & opinionated (overkill for tiny services); ORM/monolith harder to customize deeply; async was \"bolted on\" so less polished than FastAPI; you can get boxed into \"the Django way\"." },
          { vi: "<b>Nên dùng khi:</b> app full-feature cần admin, auth, DB phức tạp, ra mắt nhanh (CMS, e-commerce, SaaS, dashboard nội bộ).", en: "<b>Use when:</b> a full-feature app needs admin, auth, complex DB, and a fast launch (CMS, e-commerce, SaaS, internal dashboards)." },
          { vi: "<b>Cân nhắc khác khi:</b> chỉ cần API nhỏ/async hiệu năng cao (→ FastAPI) hoặc dịch vụ siêu nhẹ, tự lắp ráp (→ Flask).", en: "<b>Consider alternatives when:</b> you only need a small/high-performance async API (→ FastAPI) or an ultra-light, assemble-it-yourself service (→ Flask)." },
        ] },
        { type: "chips", items: ["MTV", "ORM", "QuerySet", "lazy evaluation", "migrations", "select_related", "prefetch_related", "N+1", "F() / Q()", "atomic", "middleware", "FBV / CBV", "generic views", "ModelForm", "signals", "DRF", "serializer", "ViewSet", "router", "CSRF", "XSS", "WSGI", "ASGI", "async views", "Channels", "INSTALLED_APPS"] },
      ],
    },
  ],
  flashcards: [
    { front: { vi: "Django theo mô hình gì? Map sang MVC ra sao?", en: "Which pattern does Django use? How does it map to MVC?" }, back: { vi: "<b>MTV (Model–Template–View)</b>. Template = View của MVC; View của Django = Controller của MVC; \"Controller\" thật là framework (URL dispatcher).", en: "<b>MTV (Model–Template–View)</b>. Template = MVC's View; Django's View = MVC's Controller; the real \"Controller\" is the framework (URL dispatcher)." } },
    { front: { vi: "\"Batteries-included\" nghĩa là gì?", en: "What does \"batteries-included\" mean?" }, back: { vi: "Django đi kèm sẵn ORM, admin, auth, forms, template, migrations… — làm web full-feature mà không cần lắp ghép nhiều thư viện.", en: "Django ships with the ORM, admin, auth, forms, templates, migrations… — build full-feature apps without assembling many libraries." } },
    { front: { vi: "QuerySet \"lazy\" nghĩa là gì? Khi nào chạm DB?", en: "What does a \"lazy\" QuerySet mean? When does it hit the DB?" }, back: { vi: "Tạo/filter không chạm DB. SQL chỉ chạy khi <b>evaluate</b>: lặp for, <code>list()</code>, slicing, <code>len()</code>, <code>bool()</code>. Cho phép xích filter thành 1 query.", en: "Building/filtering doesn't hit the DB. SQL runs only on <b>evaluation</b>: iterating, <code>list()</code>, slicing, <code>len()</code>, <code>bool()</code>. Lets you chain filters into one query." } },
    { front: { vi: "select_related vs prefetch_related?", en: "select_related vs prefetch_related?" }, back: { vi: "<b>select_related</b>: SQL JOIN, một query, cho <b>ForeignKey/OneToOne</b>. <b>prefetch_related</b>: query riêng + ghép trong Python, cho <b>ManyToMany/reverse FK</b>.", en: "<b>select_related</b>: SQL JOIN, one query, for <b>ForeignKey/OneToOne</b>. <b>prefetch_related</b>: separate query joined in Python, for <b>ManyToMany/reverse FK</b>." } },
    { front: { vi: "N+1 query problem là gì? Cách fix?", en: "What is the N+1 query problem? How to fix it?" }, back: { vi: "Lấy N object rồi truy cập quan hệ từng cái → 1 + N query. Fix bằng <code>select_related</code> (FK/O2O) hoặc <code>prefetch_related</code> (M2M/reverse FK).", en: "Fetch N objects then access each one's relation → 1 + N queries. Fix with <code>select_related</code> (FK/O2O) or <code>prefetch_related</code> (M2M/reverse FK)." } },
    { front: { vi: "F() và Q() object dùng để làm gì?", en: "What are F() and Q() objects for?" }, back: { vi: "<b>F()</b>: thao tác trên giá trị cột ngay trong DB (vd tăng đếm an toàn race). <b>Q()</b>: ghép điều kiện OR/AND/NOT phức tạp trong filter.", en: "<b>F()</b>: operate on a column's value in the DB (e.g. race-safe increments). <b>Q()</b>: build complex OR/AND/NOT conditions in filters." } },
    { front: { vi: "makemigrations vs migrate?", en: "makemigrations vs migrate?" }, back: { vi: "<code>makemigrations</code> sinh <b>file migration</b> từ thay đổi model. <code>migrate</code> <b>áp dụng</b> các migration đó vào DB.", en: "<code>makemigrations</code> generates <b>migration files</b> from model changes. <code>migrate</code> <b>applies</b> those migrations to the DB." } },
    { front: { vi: "Middleware là gì? Chạy theo thứ tự nào?", en: "What is middleware? In what order does it run?" }, back: { vi: "Lớp xử lý request/response bao quanh view kiểu <b>hành tây</b>: chiều request đi xuôi danh sách, chiều response đi ngược. VD: auth, CSRF, session.", en: "Layers wrapping the view like an <b>onion</b>: request goes down the list, response comes back up. E.g. auth, CSRF, sessions." } },
    { front: { vi: "transaction.atomic() làm gì?", en: "What does transaction.atomic() do?" }, back: { vi: "Gói nhiều thao tác DB thành một <b>transaction</b>: tất cả thành công thì commit, có lỗi thì <b>rollback toàn bộ</b>. Dùng làm decorator hoặc context manager.", en: "Wraps DB operations into one <b>transaction</b>: commit if all succeed, <b>roll back everything</b> on error. Usable as a decorator or context manager." } },
    { front: { vi: "DRF: serializer, ViewSet, router làm gì?", en: "DRF: what do serializer, ViewSet, router do?" }, back: { vi: "<b>Serializer</b>: model ↔ JSON + validate. <b>ViewSet</b>: gom CRUD vào một class. <b>Router</b>: tự sinh URL từ ViewSet.", en: "<b>Serializer</b>: model ↔ JSON + validation. <b>ViewSet</b>: groups CRUD into one class. <b>Router</b>: auto-generates URLs from a ViewSet." } },
    { front: { vi: "Django chống CSRF, SQLi, XSS thế nào?", en: "How does Django prevent CSRF, SQLi, XSS?" }, back: { vi: "<b>CSRF</b>: middleware + <code>{% csrf_token %}</code>. <b>SQLi</b>: ORM tham số hóa truy vấn. <b>XSS</b>: template tự động escape <code>{{ var }}</code>.", en: "<b>CSRF</b>: middleware + <code>{% csrf_token %}</code>. <b>SQLi</b>: the ORM parameterizes queries. <b>XSS</b>: templates auto-escape <code>{{ var }}</code>." } },
    { front: { vi: "WSGI vs ASGI khác gì?", en: "WSGI vs ASGI — difference?" }, back: { vi: "<b>WSGI</b>: đồng bộ, request/response cổ điển (Gunicorn). <b>ASGI</b>: bất đồng bộ, hỗ trợ async view + WebSocket (uvicorn/Daphne, Channels).", en: "<b>WSGI</b>: synchronous, classic request/response (Gunicorn). <b>ASGI</b>: asynchronous, supports async views + WebSockets (uvicorn/Daphne, Channels)." } },
    { front: { vi: "FBV vs CBV — chọn cái nào?", en: "FBV vs CBV — which to pick?" }, back: { vi: "<b>CBV/generic views</b>: ít boilerplate cho CRUD chuẩn (ListView, DetailView). <b>FBV</b>: tường minh, hợp logic đặc thù ít lặp.", en: "<b>CBV/generic views</b>: less boilerplate for standard CRUD (ListView, DetailView). <b>FBV</b>: explicit, good for bespoke one-off logic." } },
    { front: { vi: "Django vs Flask vs FastAPI một câu?", en: "Django vs Flask vs FastAPI in one line?" }, back: { vi: "<b>Django</b>: full-feature monolith (đủ đồ). <b>Flask</b>: micro, tự lắp. <b>FastAPI</b>: async-first + Pydantic + docs tự sinh cho API hiệu năng cao.", en: "<b>Django</b>: full-feature monolith (everything included). <b>Flask</b>: micro, assemble-it-yourself. <b>FastAPI</b>: async-first + Pydantic + auto docs for high-performance APIs." } },
  ],
  quiz: [
    { q: { vi: "Django dùng mô hình kiến trúc nào?", en: "Which architectural pattern does Django use?" },
      options: [{ vi: "MVC thuần", en: "Pure MVC" }, { vi: "MTV (Model–Template–View)", en: "MTV (Model–Template–View)" }, { vi: "MVVM", en: "MVVM" }, { vi: "Layered hexagonal", en: "Layered hexagonal" }], answer: 1,
      explain: { vi: "Django gọi là MTV — biến thể của MVC: Template = View của MVC, View của Django = Controller của MVC.", en: "Django calls it MTV — a variant of MVC: Template = MVC's View, Django's View = MVC's Controller." } },
    { q: { vi: "QuerySet của Django chạm DB lúc nào?", en: "When does a Django QuerySet hit the database?" },
      options: [{ vi: "Ngay khi gọi .filter()", en: "As soon as .filter() is called" }, { vi: "Khi định nghĩa model", en: "When the model is defined" }, { vi: "Khi nó được evaluate (lặp, list(), len()…)", en: "When it is evaluated (iterate, list(), len()…)" }, { vi: "Khi chạy migrate", en: "When you run migrate" }], answer: 2,
      explain: { vi: "QuerySet là lazy: chỉ chạy SQL khi evaluate — lặp for, list(), slicing, len(), bool().", en: "QuerySets are lazy: SQL runs only on evaluation — iterating, list(), slicing, len(), bool()." } },
    { q: { vi: "Để gom dữ liệu ForeignKey bằng MỘT query (SQL JOIN), dùng gì?", en: "To pull ForeignKey data in ONE query (SQL JOIN), use what?" },
      options: [{ vi: "prefetch_related", en: "prefetch_related" }, { vi: "select_related", en: "select_related" }, { vi: "values_list", en: "values_list" }, { vi: "annotate", en: "annotate" }], answer: 1,
      explain: { vi: "select_related dùng JOIN, một query, cho quan hệ đơn (ForeignKey/OneToOne). prefetch_related dùng query riêng cho M2M/reverse FK.", en: "select_related uses a JOIN, one query, for single-valued relations (ForeignKey/OneToOne). prefetch_related uses a separate query for M2M/reverse FK." } },
    { q: { vi: "Triệu chứng \"1 query lấy danh sách + N query lấy quan hệ từng phần tử\" gọi là gì?", en: "\"1 query for the list + N queries for each item's relation\" is called what?" },
      options: [{ vi: "Deadlock", en: "Deadlock" }, { vi: "N+1 query problem", en: "N+1 query problem" }, { vi: "Race condition", en: "Race condition" }, { vi: "Cache miss", en: "Cache miss" }], answer: 1,
      explain: { vi: "Đó là N+1 query problem — fix bằng select_related (FK/O2O) hoặc prefetch_related (M2M/reverse FK).", en: "That's the N+1 query problem — fixed with select_related (FK/O2O) or prefetch_related (M2M/reverse FK)." } },
    { q: { vi: "Cơ chế nào KHÔNG phải phòng vệ bảo mật mặc định của Django?", en: "Which is NOT a default Django security defense?" },
      options: [{ vi: "Chống CSRF qua middleware + token", en: "CSRF protection via middleware + token" }, { vi: "Tham số hóa truy vấn chống SQLi", en: "Query parameterization against SQLi" }, { vi: "Tự escape HTML chống XSS", en: "Auto HTML escaping against XSS" }, { vi: "Tự động chống DDoS ở tầng mạng", en: "Automatic network-layer DDoS protection" }], answer: 3,
      explain: { vi: "Django lo CSRF, SQLi, XSS sẵn. Chống DDoS là việc của tầng hạ tầng/mạng (CDN, WAF), không phải Django.", en: "Django handles CSRF, SQLi, XSS out of the box. DDoS protection is an infrastructure/network concern (CDN, WAF), not Django." } },
    { q: { vi: "Để hỗ trợ WebSocket và async views, Django cần chuẩn nào?", en: "To support WebSockets and async views, Django needs which standard?" },
      options: [{ vi: "WSGI", en: "WSGI" }, { vi: "ASGI", en: "ASGI" }, { vi: "CGI", en: "CGI" }, { vi: "FastCGI", en: "FastCGI" }], answer: 1,
      explain: { vi: "ASGI là chuẩn bất đồng bộ, cho async view + WebSocket (uvicorn/Daphne, Channels). WSGI là đồng bộ.", en: "ASGI is the asynchronous standard enabling async views + WebSockets (uvicorn/Daphne, Channels). WSGI is synchronous." } },
    { q: { vi: "Trong DRF, thành phần nào chịu trách nhiệm validate dữ liệu vào và chuyển model ↔ JSON?", en: "In DRF, which component validates input and converts model ↔ JSON?" },
      options: [{ vi: "Router", en: "Router" }, { vi: "Serializer", en: "Serializer" }, { vi: "Middleware", en: "Middleware" }, { vi: "Template", en: "Template" }], answer: 1,
      explain: { vi: "Serializer = validate + serialize/deserialize (model ↔ JSON). ViewSet lo CRUD, Router sinh URL.", en: "The serializer = validation + serialize/deserialize (model ↔ JSON). ViewSets handle CRUD, routers generate URLs." } },
    { q: { vi: "Cách an toàn để gói nhiều thao tác DB sao cho lỗi thì rollback toàn bộ?", en: "The safe way to wrap multiple DB ops so an error rolls everything back?" },
      options: [{ vi: "Gọi save() nhiều lần", en: "Call save() multiple times" }, { vi: "Dùng transaction.atomic()", en: "Use transaction.atomic()" }, { vi: "Bật DEBUG=True", en: "Set DEBUG=True" }, { vi: "Dùng select_related", en: "Use select_related" }], answer: 1,
      explain: { vi: "transaction.atomic() (decorator/context manager) commit khi mọi thao tác thành công, rollback toàn bộ khi có lỗi.", en: "transaction.atomic() (decorator/context manager) commits when all ops succeed and rolls everything back on error." } },
  ],
});
