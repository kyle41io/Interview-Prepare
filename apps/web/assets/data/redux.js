/* Redux — predictable state management (frontend) */
PREP.register({
  id: "redux",
  icon: "🔄",
  category: "frontend",
  title: { vi: "Redux — quản lý state tập trung", en: "Redux — centralized state management" },
  blurb: {
    vi: "Kho state tập trung, dễ đoán cho app lớn. Hiểu 3 nguyên tắc, luồng một chiều, async (thunk/saga), và Redux Toolkit hiện đại — cộng góc nhìn senior: nhiều khi bạn KHÔNG còn cần Redux nữa.",
    en: "A predictable, centralized state store for large apps. Master the 3 principles, the one-way flow, async (thunk/saga), and modern Redux Toolkit — plus the senior take: often you DON'T need Redux anymore.",
  },
  sections: [
    {
      id: "why",
      title: { vi: "1. Vì sao Redux tồn tại", en: "1. Why Redux exists" },
      blocks: [
        { type: "prose", vi: "App lớn lên → state dùng chung (<code>user</code>, <code>cart</code>, <code>theme</code>) bị truyền qua nhiều tầng component (<b>prop drilling</b>), và việc cập nhật khó truy vết. Redux là một <b>kho state tập trung, dễ đoán</b>: mọi state dùng chung nằm một chỗ, thay đổi theo một luồng nghiêm ngặt, và truy vết được.", en: "As an app grows, shared state (<code>user</code>, <code>cart</code>, <code>theme</code>) gets passed through many component layers (<b>prop drilling</b>), and updates become hard to trace. Redux is a <b>predictable, centralized store</b>: all shared state lives in one place, changes follow a strict flow, and is traceable." },
        { type: "list", items: [
          { vi: "<b>Prop drilling</b> — phải chuyền props qua nhiều component trung gian chẳng dùng đến chúng, chỉ để đưa dữ liệu xuống component sâu bên dưới.", en: "<b>Prop drilling</b> — passing props through many intermediate components that don't use them, just to reach a deeply nested child." },
          { vi: "<b>Cập nhật khó truy vết</b> — không rõ ai sửa state nào, khi nào, vì sao → bug khó tái hiện.", en: "<b>Updates hard to trace</b> — unclear who changed which state, when, and why → bugs are hard to reproduce." },
          { vi: "<b>Lời giải của Redux</b> — một store duy nhất, thay đổi qua luồng nghiêm ngặt và truy vết được (log mọi action).", en: "<b>Redux's answer</b> — a single store, changes via a strict and traceable flow (every action is logged)." },
        ] },
        { type: "callout", variant: "key", vi: "Redux giải hai vấn đề: <b>chia sẻ state</b> giữa các component cách xa nhau (không prop drilling) và <b>khả năng dự đoán</b> (mọi thay đổi đi qua một luồng duy nhất, log được).", en: "Redux solves two problems: <b>sharing state</b> between distant components (no prop drilling) and <b>predictability</b> (every change goes through one auditable flow)." },
      ],
    },
    {
      id: "principles",
      title: { vi: "2. Ba nguyên tắc", en: "2. The three principles" },
      blocks: [
        { type: "list", ordered: true, items: [
          { vi: "<b>Single source of truth</b> — một <b>store</b> duy nhất (một object state lớn) cho cả app.", en: "<b>Single source of truth</b> — one single <b>store</b> (one big state object) for the whole app." },
          { vi: "<b>State chỉ đọc (read-only)</b> — không sửa state trực tiếp; phải <b>dispatch một action</b> (object mô tả <i>điều đã xảy ra</i>, VD <code>{ type: 'cart/itemAdded', payload: {...} }</code>).", en: "<b>State is read-only</b> — never mutate state directly; you must <b>dispatch an action</b> (an object describing <i>what happened</i>, e.g. <code>{ type: 'cart/itemAdded', payload: {...} }</code>)." },
          { vi: "<b>Thay đổi qua pure reducer</b> — <b>reducer</b> là hàm thuần <code>(state, action) => newState</code>. Thuần = không side effect, không mutate; trả về một object state <b>mới</b> (<b>immutability</b>).", en: "<b>Changes via pure reducers</b> — a <b>reducer</b> is a pure function <code>(state, action) => newState</code>. Pure = no side effects, no mutation; returns a <b>new</b> state object (<b>immutability</b>)." },
        ] },
        { type: "table",
          headers: { vi: ["Khái niệm", "Là gì", "Quy tắc"], en: ["Concept", "What it is", "Rule"] },
          rows: [
            { vi: ["Store", "Object state lớn của cả app", "Chỉ một (single source of truth)"], en: ["Store", "The app's big state object", "Exactly one (single source of truth)"] },
            { vi: ["Action", "Object mô tả điều đã xảy ra", "Có <code>type</code>, thường có <code>payload</code>"], en: ["Action", "Object describing what happened", "Has a <code>type</code>, usually a <code>payload</code>"] },
            { vi: ["Reducer", "Hàm thuần (state, action) → state mới", "Không mutate, không side effect"], en: ["Reducer", "Pure (state, action) → new state", "No mutation, no side effects"] },
            { vi: ["Dispatch", "Cách duy nhất kích hoạt thay đổi state", "<code>store.dispatch(action)</code>"], en: ["Dispatch", "The only way to trigger a state change", "<code>store.dispatch(action)</code>"] },
          ] },
        { type: "callout", variant: "warning", vi: "<b>Immutability</b> là điểm dễ sai nhất: reducer KHÔNG được làm <code>state.x = 1</code> hay <code>arr.push(...)</code>. Phải trả về object/array <b>mới</b> (spread, map, filter). Redux Toolkit dùng Immer để bạn viết code trông như mutate mà vẫn an toàn.", en: "<b>Immutability</b> is the most common mistake: a reducer must NOT do <code>state.x = 1</code> or <code>arr.push(...)</code>. It must return a <b>new</b> object/array (spread, map, filter). Redux Toolkit uses Immer so you can write mutate-looking code that stays safe." },
      ],
    },
    {
      id: "flow",
      title: { vi: "3. Luồng dữ liệu một chiều", en: "3. The one-way data flow" },
      blocks: [
        { type: "prose", vi: "Dữ liệu chảy theo <b>một chiều (one-way)</b> qua một vòng lặp nghiêm ngặt. Đây là sơ đồ nên vẽ ra khi phỏng vấn:", en: "Data flows in <b>one direction (one-way)</b> through a strict loop. This is the diagram worth drawing in an interview:" },
        { type: "code",
          code: "UI dispatch một ACTION\n      |\n      v\nREDUCER (hàm thuan) tinh STATE moi\n      |\n      v\nSTORE cap nhat va bao cho subscriber\n      |\n      v\nUI re-render tu state moi  -> (lap lai)",
          caption: { vi: "Luồng một chiều: UI → action → reducer → store → UI (rồi lặp lại).", en: "One-way flow: UI → action → reducer → store → UI (then repeat)." } },
        { type: "list", ordered: true, items: [
          { vi: "<b>UI dispatch một action</b> — người dùng bấm nút, UI gửi một action mô tả điều đã xảy ra.", en: "<b>UI dispatches an action</b> — the user clicks a button, the UI sends an action describing what happened." },
          { vi: "<b>Reducer tính state mới</b> — hàm thuần nhận (state cũ, action) và trả về state mới.", en: "<b>Reducer computes new state</b> — the pure function takes (old state, action) and returns the new state." },
          { vi: "<b>Store cập nhật</b> — lưu state mới và báo cho mọi subscriber.", en: "<b>Store updates</b> — stores the new state and notifies all subscribers." },
          { vi: "<b>UI re-render</b> — component đọc state mới và vẽ lại, rồi vòng lặp tiếp tục.", en: "<b>UI re-renders</b> — components read the new state and repaint, then the loop continues." },
        ] },
        { type: "callout", variant: "tip", vi: "Luồng nghiêm ngặt này cho <b>time-travel debugging</b> (tua tới/lui qua từng action), cập nhật <b>dễ đoán</b>, và <b>dễ test</b> (reducer thuần → cùng input luôn ra cùng output).", en: "This strict flow enables <b>time-travel debugging</b> (step forward/back through actions), <b>predictable</b> updates, and <b>easy testing</b> (pure reducers → same input always yields same output)." },
      ],
    },
    {
      id: "async",
      title: { vi: "4. Side effect & async", en: "4. Side effects & async" },
      blocks: [
        { type: "prose", vi: "Reducer phải <b>thuần</b>, nên việc async (gọi API) không thể nằm trong reducer — nó nằm ở <b>middleware</b> (lớp chen giữa lúc dispatch action và lúc nó tới reducer).", en: "Reducers must be <b>pure</b>, so async work (API calls) cannot live in a reducer — it lives in <b>middleware</b> (a layer between dispatching an action and it reaching the reducer)." },
        { type: "table",
          headers: { vi: ["Middleware", "Cách dùng", "Ghi chú"], en: ["Middleware", "How it works", "Notes"] },
          rows: [
            { vi: ["redux-thunk", "Dispatch một <i>hàm</i> thay vì object; làm async rồi dispatch kết quả", "Mặc định, đơn giản, dùng phổ biến nhất"], en: ["redux-thunk", "Dispatch a <i>function</i> instead of an object; do async, then dispatch the result", "Default, simple, the most common choice"] },
            { vi: ["redux-saga", "Dùng generator cho luồng async phức tạp", "Mạnh nhưng nay ít dùng hơn"], en: ["redux-saga", "Uses generators for complex async flows", "Powerful but used less these days"] },
          ] },
        { type: "callout", variant: "tip", vi: "Mặc định dùng <b>thunk</b> (đi kèm Redux Toolkit). Chỉ cân nhắc <b>saga</b> khi có luồng async thật sự phức tạp (huỷ, debounce, điều phối nhiều bước) — đa số app không cần.", en: "Default to <b>thunk</b> (bundled with Redux Toolkit). Only consider <b>saga</b> for genuinely complex async flows (cancellation, debouncing, multi-step orchestration) — most apps don't need it." },
      ],
    },
    {
      id: "rtk",
      title: { vi: "5. Redux hiện đại = Redux Toolkit (RTK)", en: "5. Modern Redux = Redux Toolkit (RTK)" },
      blocks: [
        { type: "prose", vi: "Redux cổ điển nhiều <b>boilerplate</b> (hằng action type, switch reducer khổng lồ, viết tay nhiều). <b>Redux Toolkit (RTK)</b> là cách chính thức được khuyến nghị hiện nay — cắt phần lớn boilerplate đó.", en: "Classic Redux has a lot of <b>boilerplate</b> (action-type constants, giant switch reducers, lots of hand-writing). <b>Redux Toolkit (RTK)</b> is the officially recommended way today — it removes most of that boilerplate." },
        { type: "list", items: [
          { vi: "<b><code>createSlice</code></b> — sinh action + reducer cùng lúc từ một chỗ khai báo duy nhất.", en: "<b><code>createSlice</code></b> — generates actions + reducer together from a single declaration." },
          { vi: "<b>Immer</b> — cho phép viết code trông như \"mutate\" (<code>state.value++</code>) mà thực ra vẫn tạo state immutable bên dưới.", en: "<b>Immer</b> — lets you write \"mutating\" code (<code>state.value++</code>) that actually produces immutable state under the hood." },
          { vi: "<b><code>configureStore</code></b> — dựng store với middleware (thunk) và DevTools cấu hình sẵn.", en: "<b><code>configureStore</code></b> — sets up the store with middleware (thunk) and DevTools preconfigured." },
          { vi: "<b>RTK Query</b> — lớp fetch/cache dữ liệu (loading state, refetch, cache, dedup request) — cho server state.", en: "<b>RTK Query</b> — a data fetching/caching layer (loading state, refetch, caching, request dedup) — for server state." },
        ] },
        { type: "code",
          code: "const cartSlice = createSlice({\n  name: 'cart',\n  initialState: { items: [] },\n  reducers: {\n    itemAdded(state, action) {\n      // nhin nhu mutate, nhung Immer xu ly immutable\n      state.items.push(action.payload);\n    },\n  },\n});\nexport const { itemAdded } = cartSlice.actions;",
          caption: { vi: "<code>createSlice</code>: action + reducer trong một khai báo; Immer lo immutability.", en: "<code>createSlice</code>: action + reducer in one declaration; Immer handles immutability." } },
        { type: "callout", variant: "danger", vi: "Đừng viết Redux \"cổ điển\" (action type thủ công, switch reducer) trong dự án mới — dễ bị xem là lỗi thời. Mặc định luôn là <b>Redux Toolkit</b>.", en: "Don't write \"classic\" Redux (manual action types, switch reducers) in a new project — it reads as outdated. The default is always <b>Redux Toolkit</b>." },
      ],
    },
    {
      id: "senior",
      title: { vi: "6. Mấu chốt: nhiều khi KHÔNG cần Redux nữa", en: "6. Key point: often you DON'T need Redux anymore" },
      blocks: [
        { type: "prose", vi: "Góc nhìn <b>senior</b>: Redux từng là mặc định cho mọi global state, nhưng hệ sinh thái đã đổi. Hãy phân loại state trước khi chọn công cụ.", en: "The <b>senior</b> take: Redux used to be the default for all global state, but the ecosystem has changed. Classify your state before picking a tool." },
        { type: "table",
          headers: { vi: ["Loại state", "Công cụ nên dùng", "Vì sao"], en: ["State kind", "Tool to use", "Why"] },
          rows: [
            { vi: ["Server/remote state", "React Query (TanStack) hoặc RTK Query", "Cache, refetch, loading state — không cần \"global store\" thủ công"], en: ["Server/remote state", "React Query (TanStack) or RTK Query", "Caching, refetch, loading state — no hand-rolled global store"] },
            { vi: ["Global ít thay đổi (theme, auth)", "React Context + useReducer", "Hiếm khi đổi → không cần cả thư viện state"], en: ["Rarely-changing global (theme, auth)", "React Context + useReducer", "Changes rarely → no full state library needed"] },
            { vi: ["Client state, API gọn", "Zustand hoặc Jotai", "Nhẹ, ít boilerplate hơn Redux"], en: ["Client state, small API", "Zustand or Jotai", "Lightweight, less boilerplate than Redux"] },
            { vi: ["App lớn, client-state phức tạp", "Redux Toolkit", "Cần truy vết / time-travel / middleware / thống nhất team lớn"], en: ["Large app, complex client state", "Redux Toolkit", "Need tracing / time-travel / middleware / large-team consistency"] },
          ] },
        { type: "callout", variant: "soundbite", vi: "\"Phần lớn cái ta gọi là <b>global state</b> thực ra là <b>cache của server</b> — dùng React Query / RTK Query cho nó. Reach for Redux chỉ khi client-state thật sự phức tạp và cần time-travel / middleware.\"", en: "\"Most of what we call <b>global state</b> is really <b>server cache</b> — use React Query / RTK Query for that. Reach for Redux only when client state is genuinely complex and you need time-travel / middleware.\"" },
        { type: "table",
          headers: { vi: ["Ưu điểm", "Nhược điểm"], en: ["Pros", "Cons"] },
          rows: [
            { vi: ["Dễ đoán (predictable)", "Boilerplate (RTK giảm nhiều)"], en: ["Predictable", "Boilerplate (RTK cuts a lot)"] },
            { vi: ["Dễ debug + DevTools tốt (time-travel)", "Thừa với app nhỏ/vừa"], en: ["Easy to debug + great DevTools (time-travel)", "Overkill for small/medium apps"] },
            { vi: ["Dễ test (reducer thuần)", "Đường cong học"], en: ["Easy to test (pure reducers)", "Learning curve"] },
            { vi: ["Trưởng thành, hệ sinh thái lớn", "Dễ \"tập trung hoá\" state lẽ ra nên để cục bộ"], en: ["Mature, large ecosystem", "Tempts you to centralize state that should stay local"] },
          ] },
        { type: "chips", items: ["store", "action", "reducer", "dispatch", "pure function", "immutability", "single source of truth", "one-way data flow", "middleware", "redux-thunk", "redux-saga", "Redux Toolkit", "createSlice", "Immer", "configureStore", "RTK Query", "React Query", "Context", "Zustand", "Jotai", "time-travel debugging", "prop drilling"] },
      ],
    },
  ],
  flashcards: [
    { front: { vi: "Vì sao Redux tồn tại?", en: "Why does Redux exist?" }, back: { vi: "Để tránh <b>prop drilling</b> (chuyền props qua nhiều tầng) và làm state dùng chung <b>dễ đoán, truy vết được</b>: mọi thay đổi đi qua một luồng nghiêm ngặt vào một store duy nhất.", en: "To avoid <b>prop drilling</b> (passing props through many layers) and make shared state <b>predictable and traceable</b>: every change goes through one strict flow into a single store." } },
    { front: { vi: "Ba nguyên tắc của Redux?", en: "Redux's three principles?" }, back: { vi: "1) <b>Single source of truth</b> (một store). 2) <b>State chỉ đọc</b> (chỉ đổi qua dispatch action). 3) <b>Thay đổi qua pure reducer</b> <code>(state, action) => newState</code>, immutable.", en: "1) <b>Single source of truth</b> (one store). 2) <b>State is read-only</b> (change only by dispatching actions). 3) <b>Changes via pure reducers</b> <code>(state, action) => newState</code>, immutable." } },
    { front: { vi: "Reducer là gì? Quy tắc?", en: "What is a reducer? Its rules?" }, back: { vi: "Hàm thuần <code>(state, action) => newState</code>. Quy tắc: <b>không mutate</b>, <b>không side effect</b>, trả về object/array <b>mới</b>. Cùng input → cùng output.", en: "A pure function <code>(state, action) => newState</code>. Rules: <b>no mutation</b>, <b>no side effects</b>, return a <b>new</b> object/array. Same input → same output." } },
    { front: { vi: "Action là gì?", en: "What is an action?" }, back: { vi: "Một object mô tả <i>điều đã xảy ra</i>, có <code>type</code> (và thường có <code>payload</code>). VD <code>{ type: 'cart/itemAdded', payload: {...} }</code>. Là cách <b>duy nhất</b> để kích hoạt thay đổi state.", en: "An object describing <i>what happened</i>, with a <code>type</code> (and usually a <code>payload</code>). E.g. <code>{ type: 'cart/itemAdded', payload: {...} }</code>. The <b>only</b> way to trigger a state change." } },
    { front: { vi: "Vẽ luồng dữ liệu một chiều của Redux.", en: "Draw Redux's one-way data flow." }, back: { vi: "UI <b>dispatch action</b> → <b>reducer</b> (thuần) tính state mới → <b>store</b> cập nhật & báo subscriber → UI <b>re-render</b> → lặp lại.", en: "UI <b>dispatches an action</b> → <b>reducer</b> (pure) computes new state → <b>store</b> updates & notifies subscribers → UI <b>re-renders</b> → repeat." } },
    { front: { vi: "Vì sao luồng một chiều quan trọng?", en: "Why does the one-way flow matter?" }, back: { vi: "Cho <b>time-travel debugging</b>, cập nhật <b>dễ đoán</b>, và <b>dễ test</b> (reducer thuần). Luôn biết state đổi ở đâu và vì sao.", en: "It enables <b>time-travel debugging</b>, <b>predictable</b> updates, and <b>easy testing</b> (pure reducers). You always know where and why state changed." } },
    { front: { vi: "Async để ở đâu trong Redux? Vì sao?", en: "Where does async go in Redux? Why?" }, back: { vi: "Ở <b>middleware</b>, không ở reducer — vì reducer phải <b>thuần</b> (không side effect). Dùng <b>redux-thunk</b> (mặc định) hoặc <b>redux-saga</b> (luồng phức tạp).", en: "In <b>middleware</b>, not the reducer — because reducers must be <b>pure</b> (no side effects). Use <b>redux-thunk</b> (default) or <b>redux-saga</b> (complex flows)." } },
    { front: { vi: "thunk vs saga?", en: "thunk vs saga?" }, back: { vi: "<b>thunk</b>: dispatch một <i>hàm</i>, làm async rồi dispatch kết quả — đơn giản, mặc định. <b>saga</b>: dùng generator cho luồng async phức tạp — mạnh nhưng nay ít dùng.", en: "<b>thunk</b>: dispatch a <i>function</i>, do async, then dispatch the result — simple, the default. <b>saga</b>: generators for complex async flows — powerful but less used now." } },
    { front: { vi: "Redux Toolkit (RTK) giải quyết gì?", en: "What does Redux Toolkit (RTK) solve?" }, back: { vi: "Cắt <b>boilerplate</b> của Redux cổ điển. Gồm <code>createSlice</code> (action+reducer), <b>Immer</b> (viết như mutate vẫn immutable), <code>configureStore</code>, và <b>RTK Query</b>. Là cách <b>chính thức khuyến nghị</b> nay.", en: "Cuts classic Redux <b>boilerplate</b>. Includes <code>createSlice</code> (action+reducer), <b>Immer</b> (mutate-looking yet immutable), <code>configureStore</code>, and <b>RTK Query</b>. The <b>officially recommended</b> way today." } },
    { front: { vi: "Immer trong RTK làm gì?", en: "What does Immer do in RTK?" }, back: { vi: "Cho phép viết code trông như mutate (<code>state.items.push(...)</code>) trong reducer mà bên dưới vẫn tạo ra state <b>immutable</b> mới → đỡ dài dòng spread/map.", en: "Lets you write mutate-looking code (<code>state.items.push(...)</code>) in a reducer while still producing new <b>immutable</b> state under the hood → no verbose spread/map." } },
    { front: { vi: "Khi nào KHÔNG nên dùng Redux?", en: "When should you NOT use Redux?" }, back: { vi: "Server state → <b>React Query / RTK Query</b>. Global ít đổi (theme/auth) → <b>Context + useReducer</b>. Client state gọn → <b>Zustand / Jotai</b>. Redux chỉ khi client-state phức tạp + cần time-travel/middleware.", en: "Server state → <b>React Query / RTK Query</b>. Rarely-changing global (theme/auth) → <b>Context + useReducer</b>. Small client state → <b>Zustand / Jotai</b>. Redux only for complex client state + time-travel/middleware needs." } },
    { front: { vi: "Câu \"ăn điểm\" về global state?", en: "The killer line about global state?" }, back: { vi: "\"Phần lớn cái gọi là global state thực ra là <b>cache của server</b>\" → dùng React Query / RTK Query, không nhồi vào Redux store.", en: "\"Most so-called global state is really <b>server cache</b>\" → use React Query / RTK Query, don't stuff it into a Redux store." } },
  ],
  quiz: [
    { q: { vi: "Vấn đề chính nào Redux được tạo ra để giải quyết?", en: "Which core problem was Redux created to solve?" },
      options: [{ vi: "Tăng tốc render DOM", en: "Speeding up DOM rendering" }, { vi: "Prop drilling + state dùng chung khó dự đoán/truy vết", en: "Prop drilling + unpredictable, hard-to-trace shared state" }, { vi: "Thay thế HTTP fetch", en: "Replacing HTTP fetch" }, { vi: "Định tuyến (routing)", en: "Routing" }], answer: 1,
      explain: { vi: "Redux gom state dùng chung vào một store, đổi qua luồng nghiêm ngặt → hết prop drilling, dễ truy vết.", en: "Redux centralizes shared state in one store changed via a strict flow → no prop drilling, easy to trace." } },
    { q: { vi: "Reducer trong Redux PHẢI như thế nào?", en: "A Redux reducer MUST be what?" },
      options: [{ vi: "Hàm thuần, không mutate, không side effect", en: "A pure function, no mutation, no side effects" }, { vi: "Async và gọi API trực tiếp", en: "Async and call APIs directly" }, { vi: "Mutate state tại chỗ cho nhanh", en: "Mutate state in place for speed" }, { vi: "Một React component", en: "A React component" }], answer: 0,
      explain: { vi: "Reducer là hàm thuần (state, action) → state mới, immutable. Async nằm ở middleware, không ở reducer.", en: "A reducer is a pure (state, action) → new state function, immutable. Async lives in middleware, not the reducer." } },
    { q: { vi: "Thứ tự đúng của luồng dữ liệu một chiều?", en: "Correct order of the one-way data flow?" },
      options: [{ vi: "Reducer → UI → store → action", en: "Reducer → UI → store → action" }, { vi: "Store → action → UI → reducer", en: "Store → action → UI → reducer" }, { vi: "UI dispatch action → reducer → store → UI re-render", en: "UI dispatches action → reducer → store → UI re-render" }, { vi: "Action → UI → reducer → store", en: "Action → UI → reducer → store" }], answer: 2,
      explain: { vi: "UI dispatch action → reducer thuần tính state mới → store cập nhật & báo subscriber → UI re-render → lặp lại.", en: "UI dispatches an action → pure reducer computes new state → store updates & notifies → UI re-renders → repeat." } },
    { q: { vi: "Vì sao async (gọi API) KHÔNG đặt trong reducer?", en: "Why is async (API calls) NOT placed in a reducer?" },
      options: [{ vi: "Vì reducer chạy quá chậm", en: "Because reducers run too slowly" }, { vi: "Vì reducer phải thuần (không side effect) → async ở middleware", en: "Because reducers must be pure (no side effects) → async lives in middleware" }, { vi: "Vì Redux cấm fetch", en: "Because Redux forbids fetching" }, { vi: "Vì action không cho phép payload", en: "Because actions can't have payloads" }], answer: 1,
      explain: { vi: "Reducer phải thuần để dễ đoán/test. Side effect như fetch đặt ở middleware (thunk/saga).", en: "Reducers must stay pure for predictability/testing. Side effects like fetch go in middleware (thunk/saga)." } },
    { q: { vi: "Trong Redux Toolkit, vai trò của Immer là gì?", en: "In Redux Toolkit, what is Immer's role?" },
      options: [{ vi: "Gọi API và cache kết quả", en: "Call APIs and cache results" }, { vi: "Cho viết code như mutate mà vẫn tạo state immutable", en: "Let you write mutate-looking code while producing immutable state" }, { vi: "Tạo route cho app", en: "Create routes for the app" }, { vi: "Thay thế React", en: "Replace React" }], answer: 1,
      explain: { vi: "Immer cho phép <code>state.x++</code> trong reducer mà bên dưới vẫn sinh state mới immutable → bớt boilerplate.", en: "Immer lets <code>state.x++</code> in a reducer while still producing new immutable state → less boilerplate." } },
    { q: { vi: "Theo góc nhìn senior, server/remote state nên quản lý bằng gì?", en: "From the senior view, server/remote state is best managed with what?" },
      options: [{ vi: "Một Redux store viết tay đầy đủ", en: "A fully hand-written Redux store" }, { vi: "React Query (TanStack) hoặc RTK Query", en: "React Query (TanStack) or RTK Query" }, { vi: "Biến toàn cục window", en: "Global window variables" }, { vi: "localStorage thủ công", en: "Manual localStorage" }], answer: 1,
      explain: { vi: "Phần lớn \"global state\" là cache của server → React Query / RTK Query lo cache, refetch, loading thay vì Redux thủ công.", en: "Most \"global state\" is server cache → React Query / RTK Query handle caching, refetch, loading instead of hand-rolled Redux." } },
    { q: { vi: "Khi nào Redux Toolkit là lựa chọn hợp lý nhất?", en: "When is Redux Toolkit the most reasonable choice?" },
      options: [{ vi: "App nhỏ chỉ có theme sáng/tối", en: "A small app with only a light/dark theme" }, { vi: "Chỉ để fetch một danh sách sản phẩm", en: "Just to fetch a product list" }, { vi: "App lớn, client-state phức tạp, cần time-travel/middleware/thống nhất team", en: "A large app, complex client state, needing time-travel/middleware/team consistency" }, { vi: "Một trang tĩnh không có state", en: "A static page with no state" }], answer: 2,
      explain: { vi: "Reach for Redux khi client-state thật sự phức tạp và cần truy vết/time-travel/middleware; app nhỏ dùng Context/Zustand/React Query gọn hơn.", en: "Reach for Redux when client state is genuinely complex and you need tracing/time-travel/middleware; small apps are leaner with Context/Zustand/React Query." } },
  ],
});
