/* .NET — C# / ASP.NET Core / EF Core (backend interview prep) */
PREP.register({
  id: "dotnet",
  icon: "🔷",
  category: "backend",
  title: { vi: ".NET & C# (ASP.NET Core, EF Core)", en: ".NET & C# (ASP.NET Core, EF Core)" },
  blurb: {
    vi: "Stack backend phổ biến ở các công ty enterprise và shop Windows. Nắm chắc .NET hợp nhất (cross-platform), pipeline + DI của ASP.NET Core, EF Core, và vài câu C# kinh điển (DI lifetimes, async/await, value vs reference) là đủ tự tin.",
    en: "A popular backend stack at enterprises and Windows shops. Master the unified cross-platform .NET, ASP.NET Core's pipeline + DI, EF Core, and a few classic C# questions (DI lifetimes, async/await, value vs reference) and you'll be solid.",
  },
  sections: [
    {
      id: "platform",
      title: { vi: "1. .NET là gì hôm nay (runtime, CLR, IL, JIT)", en: "1. What .NET is today (runtime, CLR, IL, JIT)" },
      blocks: [
        { type: "prose", vi: "<b>.NET</b> là một <b>nền tảng phát triển</b> (runtime + thư viện + công cụ), không phải một ngôn ngữ. <b>.NET hợp nhất</b> (trước gọi là .NET Core, nay chỉ là \"<b>.NET</b>\" — .NET 5, 6, 7, 8...) là bản <b>cross-platform, mã nguồn mở</b>, chạy trên Windows / Linux / macOS. Bản cũ <b>.NET Framework</b> (1.x–4.x) chỉ chạy <b>Windows</b> và đang ở chế độ bảo trì.", en: "<b>.NET</b> is a <b>development platform</b> (runtime + libraries + tooling), not a language. The <b>unified .NET</b> (formerly .NET Core, now just \"<b>.NET</b>\" — .NET 5, 6, 7, 8...) is the <b>cross-platform, open-source</b> line running on Windows / Linux / macOS. The older <b>.NET Framework</b> (1.x–4.x) is <b>Windows-only</b> and in maintenance mode." },
        { type: "list", items: [
          { vi: "<b>C#</b> là ngôn ngữ chính (cũng có F#, VB.NET). Code C# được compile thành <b>IL</b> (Intermediate Language / CIL) — mã trung gian độc lập CPU.", en: "<b>C#</b> is the primary language (also F#, VB.NET). C# compiles to <b>IL</b> (Intermediate Language / CIL) — a CPU-independent intermediate code." },
          { vi: "<b>CLR (Common Language Runtime)</b> là máy ảo chạy IL: nó <b>JIT-compile</b> IL thành mã máy native lúc chạy, quản lý bộ nhớ, và làm <b>garbage collection</b>.", en: "<b>CLR (Common Language Runtime)</b> is the VM that runs IL: it <b>JIT-compiles</b> IL to native machine code at runtime, manages memory, and performs <b>garbage collection</b>." },
          { vi: "<b>Managed code</b> = code chạy dưới sự quản lý của CLR (tự dọn bộ nhớ, kiểm tra an toàn kiểu/biên). Trái lại là <b>unmanaged</b> (C/C++ trực tiếp).", en: "<b>Managed code</b> = code running under the CLR (automatic memory management, type/bounds safety). The opposite is <b>unmanaged</b> (raw C/C++)." },
          { vi: "<b>BCL / FCL</b> — thư viện lớp nền (collections, IO, LINQ, networking...). <b>.NET CLI</b> (<code>dotnet build</code>, <code>dotnet run</code>) là công cụ dòng lệnh; <b>NuGet</b> là trình quản lý package.", en: "<b>BCL / FCL</b> — base class libraries (collections, IO, LINQ, networking...). The <b>.NET CLI</b> (<code>dotnet build</code>, <code>dotnet run</code>) is the command-line tool; <b>NuGet</b> is the package manager." },
        ] },
        { type: "callout", variant: "soundbite", vi: "Câu chốt: \"<b>.NET Framework là Windows-only và đã đóng băng; .NET hợp nhất (Core trở đi) là cross-platform, mã nguồn mở, hiệu năng cao</b> — dự án mới luôn chọn .NET hiện đại.\"", en: "One-liner: \"<b>.NET Framework is Windows-only and frozen; the unified .NET (Core onward) is cross-platform, open-source, high-performance</b> — new projects always use modern .NET.\"" },
        { type: "callout", variant: "tip", vi: "Phân biệt: <b>JIT</b> (Just-In-Time) compile lúc chạy — mặc định. <b>AOT</b> (Ahead-Of-Time, Native AOT) compile trước thành native lúc build → khởi động nhanh, ít RAM, không cần JIT (tốt cho serverless/CLI), đổi lại mất một số tính năng reflection.", en: "Distinguish: <b>JIT</b> (Just-In-Time) compiles at runtime — the default. <b>AOT</b> (Ahead-Of-Time, Native AOT) compiles to native at build time → fast startup, low memory, no JIT needed (great for serverless/CLI), at the cost of some reflection features." },
      ],
    },
    {
      id: "csharp",
      title: { vi: "2. C# cốt lõi (câu hỏi hay gặp)", en: "2. Core C# (frequently asked)" },
      blocks: [
        { type: "table",
          headers: { vi: ["Khái niệm", "Ý chính"], en: ["Concept", "Key point"] },
          rows: [
            { vi: ["Value vs Reference type", "<code>struct</code>/<code>int</code>/<code>bool</code> nằm trên stack, copy theo <b>giá trị</b>. <code>class</code>/<code>string</code>/array nằm trên heap, copy theo <b>tham chiếu</b>."], en: ["Value vs Reference type", "<code>struct</code>/<code>int</code>/<code>bool</code> live on the stack, copied <b>by value</b>. <code>class</code>/<code>string</code>/array live on the heap, copied <b>by reference</b>."] },
            { vi: ["async / await + Task", "Bất đồng bộ <b>không chặn thread</b>; <code>Task</code> đại diện cho công việc tương lai. Khác với tạo thread mới."], en: ["async / await + Task", "Asynchrony that <b>doesn't block a thread</b>; a <code>Task</code> represents future work. Different from spawning a thread."] },
            { vi: ["IDisposable / using", "Giải phóng tài nguyên unmanaged (file, socket, DB connection) tất định qua <code>Dispose()</code>; <code>using</code> tự gọi khi ra khỏi scope."], en: ["IDisposable / using", "Deterministically release unmanaged resources (files, sockets, DB connections) via <code>Dispose()</code>; <code>using</code> calls it on scope exit."] },
            { vi: ["LINQ", "Truy vấn collection bằng cú pháp khai báo (<code>.Where().Select()</code>). Hoãn thực thi (deferred)."], en: ["LINQ", "Query collections declaratively (<code>.Where().Select()</code>). Deferred execution."] },
            { vi: ["Delegate / event", "Con trỏ hàm có kiểu (<code>Func</code>, <code>Action</code>); event là cơ chế pub/sub dựa trên delegate."], en: ["Delegate / event", "Typed function pointers (<code>Func</code>, <code>Action</code>); events are a delegate-based pub/sub mechanism."] },
            { vi: ["Interface vs abstract class", "Interface = hợp đồng, đa kế thừa, không state. Abstract class = lớp cơ sở chung có code + field, đơn kế thừa."], en: ["Interface vs abstract class", "Interface = contract, multiple-inheritable, no state. Abstract class = shared base with code + fields, single inheritance."] },
            { vi: ["Property", "Cú pháp <code>get/set</code> bọc field; cho phép validate, computed, auto-property <code>{ get; set; }</code>."], en: ["Property", "<code>get/set</code> syntax wrapping a field; allows validation, computed values, auto-properties <code>{ get; set; }</code>."] },
          ] },
        { type: "code",
          code: "// Value type: copy theo giá trị\nint a = 5;\nint b = a;      // b la ban sao\nb = 99;         // a van = 5\n\n// Reference type: copy tham chieu\nvar p1 = new Person { Name = \"An\" };\nvar p2 = p1;    // p2 tro toi CUNG object\np2.Name = \"Binh\";   // p1.Name cung = \"Binh\"\n\n// async/await: khong chan thread\npublic async Task<string> GetDataAsync(HttpClient http) {\n    string html = await http.GetStringAsync(\"https://api.example.com\");\n    return html;   // thread duoc tra ve pool trong luc cho I/O\n}\n\n// using: tu dong Dispose()\nusing (var conn = new SqlConnection(cs)) {\n    conn.Open();\n}   // conn.Dispose() goi tu dong o day",
          caption: { vi: "Value vs reference, async/await, và using trong C#.", en: "Value vs reference, async/await, and using in C#." } },
        { type: "callout", variant: "key", vi: "<b>async/await ≠ multithreading.</b> Với I/O-bound (gọi DB/HTTP/file), <code>await</code> <b>nhả thread về pool</b> trong lúc chờ — không tạo thread mới. Đó là lý do server async chịu tải đồng thời cao mà ít thread.", en: "<b>async/await ≠ multithreading.</b> For I/O-bound work (DB/HTTP/file), <code>await</code> <b>returns the thread to the pool</b> while waiting — it doesn't spawn a thread. That's why async servers handle high concurrency with few threads." },
        { type: "callout", variant: "warning", vi: "<code>string</code> là reference type nhưng <b>immutable</b> — mỗi phép nối tạo chuỗi mới. Nối nhiều trong vòng lặp → dùng <code>StringBuilder</code>. Tránh <b>async void</b> (trừ event handler) vì nuốt exception.", en: "<code>string</code> is a reference type but <b>immutable</b> — each concatenation makes a new string. Concatenating in a loop → use <code>StringBuilder</code>. Avoid <b>async void</b> (except event handlers) — it swallows exceptions." },
      ],
    },
    {
      id: "aspnet",
      title: { vi: "3. ASP.NET Core: pipeline, middleware, DI", en: "3. ASP.NET Core: pipeline, middleware, DI" },
      blocks: [
        { type: "prose", vi: "<b>ASP.NET Core</b> là framework web cross-platform. Một request đi qua <b>pipeline</b> gồm các <b>middleware</b> nối tiếp (auth, logging, routing, static files...), mỗi middleware có thể xử lý rồi gọi cái kế tiếp hoặc \"short-circuit\". Server mặc định là <b>Kestrel</b> (nhanh, cross-platform), thường đặt sau reverse proxy (Nginx/IIS).", en: "<b>ASP.NET Core</b> is a cross-platform web framework. A request flows through a <b>pipeline</b> of chained <b>middleware</b> (auth, logging, routing, static files...); each middleware can handle it and call the next or short-circuit. The default server is <b>Kestrel</b> (fast, cross-platform), usually behind a reverse proxy (Nginx/IIS)." },
        { type: "list", ordered: true, items: [
          { vi: "Request vào → middleware <b>theo thứ tự đăng ký</b> (thứ tự QUAN TRỌNG: ví dụ <code>UseAuthentication</code> phải trước <code>UseAuthorization</code>).", en: "Request comes in → middleware runs <b>in registration order</b> (order MATTERS: e.g. <code>UseAuthentication</code> must precede <code>UseAuthorization</code>)." },
          { vi: "Routing khớp endpoint → controller/minimal API xử lý → tạo response.", en: "Routing matches an endpoint → controller/minimal API handles it → produces a response." },
          { vi: "Response đi ngược qua các middleware ra ngoài.", en: "The response travels back out through the middleware." },
        ] },
        { type: "prose", vi: "Tâm điểm là <b>Dependency Injection (DI)</b> tích hợp sẵn. Bạn <b>đăng ký</b> service vào container (qua interface), framework <b>tự inject</b> vào constructor. Ba <b>lifetime</b> (vòng đời) là câu hỏi kinh điển:", en: "Central to it is the built-in <b>Dependency Injection (DI)</b>. You <b>register</b> services in the container (against interfaces) and the framework <b>injects</b> them via constructors. The three <b>lifetimes</b> are a classic interview question:" },
        { type: "table",
          headers: { vi: ["Lifetime", "Tạo instance khi nào", "Dùng cho"], en: ["Lifetime", "When an instance is created", "Use for"] },
          rows: [
            { vi: ["<b>Transient</b>", "Mỗi lần được yêu cầu → instance mới", "Service nhẹ, không state, rẻ để tạo"], en: ["<b>Transient</b>", "Every time it's requested → new instance", "Lightweight, stateless, cheap-to-create services"] },
            { vi: ["<b>Scoped</b>", "Một instance mỗi <b>request HTTP</b> (mỗi scope)", "DbContext, unit-of-work, dữ liệu theo request"], en: ["<b>Scoped</b>", "One instance per <b>HTTP request</b> (per scope)", "DbContext, unit-of-work, per-request data"] },
            { vi: ["<b>Singleton</b>", "Một instance duy nhất cho cả app", "Cache, config, service stateless dùng chung; phải thread-safe"], en: ["<b>Singleton</b>", "One single instance for the whole app", "Caches, config, shared stateless services; must be thread-safe"] },
          ] },
        { type: "code",
          code: "var builder = WebApplication.CreateBuilder(args);\n\n// Dang ky service theo lifetime\nbuilder.Services.AddTransient<IEmailSender, SmtpEmailSender>();\nbuilder.Services.AddScoped<IOrderRepository, OrderRepository>();\nbuilder.Services.AddSingleton<IClock, SystemClock>();\nbuilder.Services.AddDbContext<AppDbContext>();  // Scoped mac dinh\n\nvar app = builder.Build();\n\n// Pipeline: thu tu middleware QUAN TRONG\napp.UseAuthentication();\napp.UseAuthorization();\n\n// Minimal API endpoint — DI inject thang vao handler\napp.MapGet(\"/orders/{id}\", async (int id, IOrderRepository repo) => {\n    var order = await repo.GetByIdAsync(id);\n    return order is null ? Results.NotFound() : Results.Ok(order);\n});\n\napp.Run();",
          caption: { vi: "Đăng ký DI (Transient/Scoped/Singleton) + một minimal API endpoint.", en: "DI registration (Transient/Scoped/Singleton) + a minimal API endpoint." } },
        { type: "callout", variant: "soundbite", vi: "Nhớ DI lifetimes: \"<b>Transient = mỗi lần inject một cái mới; Scoped = một cái mỗi request; Singleton = một cái cho cả app.</b>\" Bẫy kinh điển: <b>không inject Scoped (như DbContext) vào Singleton</b> → captive dependency, lỗi/state rò rỉ.", en: "Remember DI lifetimes: \"<b>Transient = new one each injection; Scoped = one per request; Singleton = one for the whole app.</b>\" Classic trap: <b>don't inject a Scoped (like DbContext) into a Singleton</b> → captive dependency, bugs/leaked state." },
        { type: "list", items: [
          { vi: "<b>Minimal APIs</b> — khai báo endpoint gọn (<code>app.MapGet/MapPost</code>), ít boilerplate, hợp microservice nhỏ. <b>MVC controllers</b> — lớp <code>[ApiController]</code> + attribute routing, hợp app lớn cần cấu trúc, filters, model validation rõ ràng.", en: "<b>Minimal APIs</b> — terse endpoint declarations (<code>app.MapGet/MapPost</code>), little boilerplate, great for small services. <b>MVC controllers</b> — <code>[ApiController]</code> classes + attribute routing, good for larger apps needing structure, filters, explicit model validation." },
          { vi: "<b>Configuration & Options</b> — đọc cấu hình từ <code>appsettings.json</code>, env vars, secrets... gộp theo thứ tự ưu tiên. <b>Options pattern</b> (<code>IOptions&lt;T&gt;</code>) bind cấu hình vào lớp POCO mạnh kiểu.", en: "<b>Configuration & Options</b> — read config from <code>appsettings.json</code>, env vars, secrets... layered by precedence. The <b>Options pattern</b> (<code>IOptions&lt;T&gt;</code>) binds config into strongly-typed POCO classes." },
          { vi: "<b>Model binding</b> — tự ánh xạ dữ liệu request (route/query/body/header) vào tham số/đối tượng; kèm <b>model validation</b> (Data Annotations).", en: "<b>Model binding</b> — automatically maps request data (route/query/body/header) into parameters/objects; with <b>model validation</b> (Data Annotations)." },
        ] },
      ],
    },
    {
      id: "efcore",
      title: { vi: "4. Entity Framework Core (ORM)", en: "4. Entity Framework Core (ORM)" },
      blocks: [
        { type: "prose", vi: "<b>EF Core</b> là ORM chính thức: ánh xạ class C# ↔ bảng DB, cho phép truy vấn bằng <b>LINQ</b> thay vì SQL thô. <b>DbContext</b> là cửa ngõ: nó đại diện cho một <b>unit of work</b>, theo dõi thay đổi (<b>change tracking</b>), và lưu bằng <code>SaveChanges()</code>.", en: "<b>EF Core</b> is the official ORM: maps C# classes ↔ DB tables, letting you query with <b>LINQ</b> instead of raw SQL. The <b>DbContext</b> is the gateway: it represents a <b>unit of work</b>, tracks changes (<b>change tracking</b>), and persists them via <code>SaveChanges()</code>." },
        { type: "list", items: [
          { vi: "<b>Migrations</b> — phiên bản hóa schema DB theo model code (<code>dotnet ef migrations add</code>, <code>database update</code>). Code-first là cách dùng phổ biến.", en: "<b>Migrations</b> — version the DB schema from your code model (<code>dotnet ef migrations add</code>, <code>database update</code>). Code-first is the common approach." },
          { vi: "<b>Change tracking</b> — DbContext nhớ entity nào đã sửa/thêm/xóa rồi sinh SQL tối thiểu khi SaveChanges. Dùng <code>AsNoTracking()</code> cho query chỉ-đọc để nhanh hơn.", en: "<b>Change tracking</b> — the DbContext remembers which entities were modified/added/deleted and emits minimal SQL on SaveChanges. Use <code>AsNoTracking()</code> for read-only queries to go faster." },
          { vi: "<b>Eager loading</b> (<code>.Include()</code>) — nạp luôn các quan hệ trong một query. <b>Lazy loading</b> — nạp quan hệ khi truy cập lần đầu (dễ gây <b>N+1</b>). <b>Explicit loading</b> — gọi nạp thủ công.", en: "<b>Eager loading</b> (<code>.Include()</code>) — load related data in the same query. <b>Lazy loading</b> — loads relations on first access (easily causes <b>N+1</b>). <b>Explicit loading</b> — load on demand manually." },
        ] },
        { type: "callout", variant: "danger", vi: "<b>Vấn đề N+1:</b> lặp qua N entity rồi truy cập navigation property mỗi vòng → 1 query lấy danh sách + N query con = N+1 lần round-trip DB. Sửa bằng <code>.Include()</code> (eager loading) để gộp lại còn 1-2 query.", en: "<b>N+1 problem:</b> looping over N entities and touching a navigation property each iteration → 1 query for the list + N child queries = N+1 DB round-trips. Fix with <code>.Include()</code> (eager loading) to collapse into 1-2 queries." },
        { type: "table",
          headers: { vi: ["", "IEnumerable<T>", "IQueryable<T>"], en: ["", "IEnumerable<T>", "IQueryable<T>"] },
          rows: [
            { vi: ["Thực thi ở đâu", "Trong bộ nhớ (client-side)", "Trên DB server (dịch ra SQL)"], en: ["Executes where", "In memory (client-side)", "On the DB server (translated to SQL)"] },
            { vi: ["Lọc/sort", "Kéo HẾT dữ liệu về rồi lọc trong app", "Đẩy <code>Where/OrderBy</code> xuống SQL — chỉ lấy cần thiết"], en: ["Filter/sort", "Pull ALL rows then filter in the app", "Pushes <code>Where/OrderBy</code> into SQL — fetch only what's needed"] },
            { vi: ["Khi nào dùng", "Dữ liệu đã ở trong bộ nhớ (List, array)", "Truy vấn nguồn ngoài (EF/DB) — hiệu quả hơn"], en: ["When to use", "Data already in memory (List, array)", "Querying an external source (EF/DB) — more efficient"] },
            { vi: ["Thực thi", "Deferred (hoãn) cho LINQ", "Deferred — chỉ chạy SQL khi liệt kê (ToList/foreach)"], en: ["Execution", "Deferred for LINQ", "Deferred — SQL runs only on enumeration (ToList/foreach)"] },
          ] },
        { type: "code",
          code: "public class AppDbContext : DbContext {\n    public DbSet<Order> Orders => Set<Order>();\n}\n\n// IQueryable: Where duoc DICH RA SQL, chay tren DB\nIQueryable<Order> q = db.Orders\n    .Where(o => o.Total > 100)      // -> WHERE Total > 100\n    .Include(o => o.Customer)       // eager load, tranh N+1\n    .OrderByDescending(o => o.Date);\n\n// Deferred: SQL chi chay o day\nList<Order> result = await q.AsNoTracking().ToListAsync();\n\n// BAY N+1: moi vong lap = 1 query Customer (neu lazy load)\nforeach (var o in db.Orders.ToList())\n    Console.WriteLine(o.Customer.Name);  // N+1!",
          caption: { vi: "EF Core: DbContext, IQueryable dịch ra SQL, Include chống N+1, deferred execution.", en: "EF Core: DbContext, IQueryable translated to SQL, Include to fight N+1, deferred execution." } },
        { type: "callout", variant: "key", vi: "Câu chốt: \"<b>IQueryable đẩy filter xuống DB (server-side); IEnumerable lọc trong bộ nhớ (client-side).</b>\" Vô tình ép sang IEnumerable sớm (ví dụ <code>.ToList()</code> rồi <code>.Where()</code>) sẽ kéo cả bảng về app — bug hiệu năng kinh điển.", en: "One-liner: \"<b>IQueryable pushes filtering down to the DB (server-side); IEnumerable filters in memory (client-side).</b>\" Accidentally switching to IEnumerable early (e.g. <code>.ToList()</code> then <code>.Where()</code>) drags the whole table into the app — a classic perf bug." },
      ],
    },
    {
      id: "webapi",
      title: { vi: "5. Web API: routing, binding, auth", en: "5. Web APIs: routing, binding, auth" },
      blocks: [
        { type: "list", items: [
          { vi: "<b>Controllers</b> — lớp kế thừa <code>ControllerBase</code> với <code>[ApiController]</code>; action trả <code>IActionResult</code>/<code>ActionResult&lt;T&gt;</code> (Ok, NotFound, BadRequest...).", en: "<b>Controllers</b> — classes deriving <code>ControllerBase</code> with <code>[ApiController]</code>; actions return <code>IActionResult</code>/<code>ActionResult&lt;T&gt;</code> (Ok, NotFound, BadRequest...)." },
          { vi: "<b>Attribute routing</b> — gắn route bằng attribute: <code>[HttpGet(\"api/orders/{id}\")]</code>. Rõ ràng, RESTful.", en: "<b>Attribute routing</b> — define routes via attributes: <code>[HttpGet(\"api/orders/{id}\")]</code>. Explicit, RESTful." },
          { vi: "<b>Model binding</b> — ánh xạ dữ liệu request vào tham số: <code>[FromBody]</code>, <code>[FromQuery]</code>, <code>[FromRoute]</code>. Với <code>[ApiController]</code>, binding + validation tự động (trả 400 nếu model invalid).", en: "<b>Model binding</b> — maps request data into parameters: <code>[FromBody]</code>, <code>[FromQuery]</code>, <code>[FromRoute]</code>. With <code>[ApiController]</code>, binding + validation are automatic (returns 400 on an invalid model)." },
          { vi: "<b>Authentication vs Authorization</b> — <i>authn</i> = bạn là ai (login), <i>authz</i> = bạn được làm gì (<code>[Authorize]</code>, role/policy).", en: "<b>Authentication vs Authorization</b> — <i>authn</i> = who you are (login), <i>authz</i> = what you may do (<code>[Authorize]</code>, roles/policies)." },
          { vi: "<b>JWT (JSON Web Token)</b> — token stateless ký số, gửi qua header <code>Authorization: Bearer</code>; server xác thực bằng chữ ký, không cần lưu session — hợp API/microservice.", en: "<b>JWT (JSON Web Token)</b> — a signed stateless token sent in the <code>Authorization: Bearer</code> header; the server validates the signature without storing a session — great for APIs/microservices." },
          { vi: "<b>ASP.NET Core Identity</b> — hệ thống quản lý user/password/role/login dựng sẵn (thường dùng với cookie cho web app, hoặc kết hợp JWT cho API).", en: "<b>ASP.NET Core Identity</b> — a built-in system for users/passwords/roles/login (typically cookie-based for web apps, or combined with JWT for APIs)." },
        ] },
        { type: "code",
          code: "[ApiController]\n[Route(\"api/[controller]\")]\npublic class OrdersController : ControllerBase {\n    private readonly IOrderRepository _repo;\n    public OrdersController(IOrderRepository repo) => _repo = repo;  // DI\n\n    [HttpGet(\"{id}\")]\n    [Authorize(Roles = \"Admin\")]\n    public async Task<ActionResult<Order>> Get(int id) {\n        var order = await _repo.GetByIdAsync(id);\n        return order is null ? NotFound() : Ok(order);\n    }\n\n    [HttpPost]\n    public async Task<IActionResult> Create([FromBody] OrderDto dto) {\n        // [ApiController] tu validate dto -> tra 400 neu invalid\n        var created = await _repo.AddAsync(dto);\n        return CreatedAtAction(nameof(Get), new { id = created.Id }, created);\n    }\n}",
          caption: { vi: "MVC controller: attribute routing, DI qua constructor, model binding, [Authorize].", en: "MVC controller: attribute routing, constructor DI, model binding, [Authorize]." } },
      ],
    },
    {
      id: "gc-perf",
      title: { vi: "6. Garbage collection & hiệu năng runtime", en: "6. Garbage collection & runtime performance" },
      blocks: [
        { type: "prose", vi: "CLR có <b>garbage collector</b> tự dọn object không còn tham chiếu trên <b>managed heap</b> — bạn không free thủ công. GC dùng mô hình <b>thế hệ (generational)</b> dựa trên giả thuyết \"đa số object chết trẻ\":", en: "The CLR has a <b>garbage collector</b> that automatically reclaims unreferenced objects on the <b>managed heap</b> — you don't free manually. The GC uses a <b>generational</b> model based on the \"most objects die young\" hypothesis:" },
        { type: "list", items: [
          { vi: "<b>Gen 0</b> — object mới, nhỏ, ngắn hạn. GC gen 0 chạy thường xuyên & rất nhanh.", en: "<b>Gen 0</b> — new, small, short-lived objects. Gen 0 collection runs often and is very fast." },
          { vi: "<b>Gen 1</b> — vùng đệm: object sống sót gen 0 chuyển lên đây.", en: "<b>Gen 1</b> — a buffer: objects surviving gen 0 are promoted here." },
          { vi: "<b>Gen 2</b> — object sống lâu (cache, singleton). GC gen 2 (full) ít chạy nhưng tốn nhất. Object lớn vào <b>LOH (Large Object Heap)</b>.", en: "<b>Gen 2</b> — long-lived objects (caches, singletons). Gen 2 (full) collection is rare but most expensive. Large objects go to the <b>LOH (Large Object Heap)</b>." },
        ] },
        { type: "callout", variant: "tip", vi: "GC tự lo <b>managed memory</b>, nhưng tài nguyên <b>unmanaged</b> (file, socket, DB connection) phải giải phóng tất định qua <code>IDisposable</code>/<code>using</code> — GC không biết khi nào nên đóng chúng. Có chế độ <b>Workstation vs Server GC</b> (server GC dùng nhiều thread, tốt cho web server tải cao).", en: "GC handles <b>managed memory</b>, but <b>unmanaged</b> resources (files, sockets, DB connections) must be released deterministically via <code>IDisposable</code>/<code>using</code> — the GC can't know when to close them. There are <b>Workstation vs Server GC</b> modes (server GC is multi-threaded, good for high-load web servers)." },
      ],
    },
    {
      id: "compare",
      title: { vi: "7. .NET vs Node.js vs Java/Spring + khi nào dùng", en: "7. .NET vs Node.js vs Java/Spring + when to use" },
      blocks: [
        { type: "table",
          headers: { vi: ["Tiêu chí", ".NET (C#)", "Node.js", "Java / Spring"], en: ["Criterion", ".NET (C#)", "Node.js", "Java / Spring"] },
          rows: [
            { vi: ["Ngôn ngữ", "C# (static typing)", "JavaScript/TS", "Java (static typing)"], en: ["Language", "C# (static typing)", "JavaScript/TS", "Java (static typing)"] },
            { vi: ["Runtime", "CLR (JIT/AOT)", "V8 (single-thread event loop)", "JVM (JIT)"], en: ["Runtime", "CLR (JIT/AOT)", "V8 (single-thread event loop)", "JVM (JIT)"] },
            { vi: ["Mô hình concurrency", "Multi-thread + async/await", "Event loop, async I/O", "Multi-thread + async"], en: ["Concurrency model", "Multi-thread + async/await", "Event loop, async I/O", "Multi-thread + async"] },
            { vi: ["Hiệu năng", "Rất cao (CPU + I/O)", "Tốt cho I/O, yếu CPU nặng", "Rất cao"], en: ["Performance", "Very high (CPU + I/O)", "Great for I/O, weak for heavy CPU", "Very high"] },
            { vi: ["Hệ sinh thái", "Enterprise, Azure, Windows", "JS/web, npm khổng lồ", "Enterprise, rất trưởng thành"], en: ["Ecosystem", "Enterprise, Azure, Windows", "JS/web, huge npm", "Enterprise, very mature"] },
            { vi: ["Hợp với", "API hiệu năng cao, enterprise, Windows shop", "Realtime, microservice nhẹ, full-stack JS", "Hệ thống lớn, Android, fintech"], en: ["Best fit", "High-perf APIs, enterprise, Windows shops", "Realtime, light microservices, full-stack JS", "Large systems, Android, fintech"] },
          ] },
        { type: "list", items: [
          { vi: "<b>Ưu điểm .NET:</b> hiệu năng cao (top trong benchmark như TechEmpower), static typing bắt lỗi sớm, tooling/IDE xuất sắc (Visual Studio/Rider), tích hợp Azure, một ngôn ngữ cho web/desktop/mobile (MAUI)/cloud/game (Unity).", en: "<b>.NET pros:</b> high performance (top in benchmarks like TechEmpower), static typing catches errors early, excellent tooling/IDE (Visual Studio/Rider), Azure integration, one language across web/desktop/mobile (MAUI)/cloud/game (Unity)." },
          { vi: "<b>Nhược điểm:</b> gắn nhiều với hệ sinh thái Microsoft; lịch sử Windows-only khiến vài người còn định kiến (dù .NET hiện đại đã cross-platform); cộng đồng mảng startup nhỏ hơn Node.", en: "<b>Cons:</b> tied to the Microsoft ecosystem; historical Windows-only reputation lingers (though modern .NET is cross-platform); a smaller startup-scene community than Node." },
          { vi: "<b>Khi nào chọn .NET:</b> backend enterprise, API hiệu năng cao, shop đã dùng Windows/Azure/SQL Server, team biết C#, hoặc cần một stack thống nhất nhiều nền tảng.", en: "<b>When to choose .NET:</b> enterprise backends, high-performance APIs, shops already on Windows/Azure/SQL Server, teams that know C#, or when you want one unified stack across platforms." },
        ] },
      ],
    },
    {
      id: "terms",
      title: { vi: "8. Thuật ngữ & checklist", en: "8. Terms & checklist" },
      blocks: [
        { type: "chips", items: ["CLR", "IL / CIL", "JIT", "AOT", "managed code", "garbage collection", "Gen 0/1/2", "C#", "value vs reference type", "async / await", "Task", "IDisposable / using", "LINQ", "delegate / event", "DI container", "Transient / Scoped / Singleton", "middleware", "Kestrel", "minimal API", "MVC controller", "EF Core", "DbContext", "migrations", "N+1", "IQueryable vs IEnumerable", "deferred execution", "JWT", "Identity", "attribute routing", "model binding"] },
        { type: "callout", variant: "tip", vi: "Ôn theo trục: (1) nền tảng (CLR/IL/JIT/GC), (2) C# core (value/ref, async, IDisposable, LINQ), (3) ASP.NET Core (pipeline + DI lifetimes), (4) EF Core (N+1, IQueryable). Bốn câu gần như chắc chắn bị hỏi: <b>DI lifetimes</b>, <b>async/await vs thread</b>, <b>value vs reference</b>, <b>IQueryable vs IEnumerable</b>.", en: "Revise along: (1) platform (CLR/IL/JIT/GC), (2) C# core (value/ref, async, IDisposable, LINQ), (3) ASP.NET Core (pipeline + DI lifetimes), (4) EF Core (N+1, IQueryable). Four near-certain questions: <b>DI lifetimes</b>, <b>async/await vs threads</b>, <b>value vs reference</b>, <b>IQueryable vs IEnumerable</b>." },
      ],
    },
  ],
  flashcards: [
    { front: { vi: ".NET Framework vs .NET (Core trở đi) khác gì?", en: ".NET Framework vs .NET (Core onward)?" }, back: { vi: "<b>.NET Framework</b>: Windows-only, đóng băng (chỉ vá lỗi). <b>.NET hợp nhất</b> (Core → .NET 5/6/7/8): <b>cross-platform, mã nguồn mở, hiệu năng cao</b>. Dự án mới dùng .NET hiện đại.", en: "<b>.NET Framework</b>: Windows-only, frozen (patches only). <b>Unified .NET</b> (Core → .NET 5/6/7/8): <b>cross-platform, open-source, high-performance</b>. New projects use modern .NET." } },
    { front: { vi: "CLR, IL, JIT là gì?", en: "What are the CLR, IL, JIT?" }, back: { vi: "<b>IL</b>: mã trung gian C# compile ra. <b>CLR</b>: runtime chạy IL, quản lý bộ nhớ + GC. <b>JIT</b>: compile IL → mã máy native lúc chạy.", en: "<b>IL</b>: intermediate code C# compiles to. <b>CLR</b>: runtime that runs IL, manages memory + GC. <b>JIT</b>: compiles IL → native machine code at runtime." } },
    { front: { vi: "Value type vs reference type?", en: "Value type vs reference type?" }, back: { vi: "<b>Value</b> (struct, int, bool): trên stack, copy theo giá trị. <b>Reference</b> (class, string, array): trên heap, biến giữ tham chiếu, copy → cùng object.", en: "<b>Value</b> (struct, int, bool): on the stack, copied by value. <b>Reference</b> (class, string, array): on the heap, variable holds a reference, copy → same object." } },
    { front: { vi: "async/await khác tạo thread thế nào?", en: "How does async/await differ from spawning a thread?" }, back: { vi: "<code>await</code> trên I/O <b>nhả thread về pool</b> trong lúc chờ, không tạo thread mới. Cho concurrency cao với ít thread. Tạo thread tốn RAM + context-switch.", en: "<code>await</code> on I/O <b>returns the thread to the pool</b> while waiting; it doesn't spawn a thread. Enables high concurrency with few threads. Spawning threads costs RAM + context switches." } },
    { front: { vi: "DI lifetimes: Transient / Scoped / Singleton?", en: "DI lifetimes: Transient / Scoped / Singleton?" }, back: { vi: "<b>Transient</b>: mới mỗi lần inject. <b>Scoped</b>: một cái mỗi request HTTP (DbContext). <b>Singleton</b>: một cái cho cả app (phải thread-safe).", en: "<b>Transient</b>: new each injection. <b>Scoped</b>: one per HTTP request (DbContext). <b>Singleton</b>: one for the whole app (must be thread-safe)." } },
    { front: { vi: "Captive dependency là bẫy gì trong DI?", en: "What's the captive dependency trap in DI?" }, back: { vi: "Inject service <b>Scoped/Transient vào Singleton</b> → nó bị giữ sống suốt đời app (vd DbContext không bao giờ được làm mới) → state rò rỉ, lỗi đa luồng.", en: "Injecting a <b>Scoped/Transient into a Singleton</b> → it's held alive for the app's lifetime (e.g. a DbContext never refreshed) → leaked state, threading bugs." } },
    { front: { vi: "Middleware pipeline trong ASP.NET Core?", en: "The middleware pipeline in ASP.NET Core?" }, back: { vi: "Chuỗi component xử lý request theo <b>thứ tự đăng ký</b>; mỗi cái có thể xử lý + gọi tiếp hoặc short-circuit. Thứ tự QUAN TRỌNG (<code>UseAuthentication</code> trước <code>UseAuthorization</code>).", en: "A chain of components processing the request <b>in registration order</b>; each can handle + call next or short-circuit. Order MATTERS (<code>UseAuthentication</code> before <code>UseAuthorization</code>)." } },
    { front: { vi: "Minimal API vs MVC controller?", en: "Minimal API vs MVC controller?" }, back: { vi: "<b>Minimal API</b>: gọn (<code>app.MapGet</code>), ít boilerplate, hợp service nhỏ. <b>MVC controller</b>: có cấu trúc, filters, validation rõ — hợp app lớn.", en: "<b>Minimal API</b>: terse (<code>app.MapGet</code>), little boilerplate, good for small services. <b>MVC controller</b>: structured, filters, explicit validation — good for large apps." } },
    { front: { vi: "DbContext trong EF Core làm gì?", en: "What does the EF Core DbContext do?" }, back: { vi: "Cửa ngõ tới DB: là <b>unit of work</b>, <b>theo dõi thay đổi</b> (change tracking), expose <code>DbSet&lt;T&gt;</code>, lưu bằng <code>SaveChanges()</code>. Đăng ký Scoped.", en: "Gateway to the DB: a <b>unit of work</b>, does <b>change tracking</b>, exposes <code>DbSet&lt;T&gt;</code>, persists via <code>SaveChanges()</code>. Registered as Scoped." } },
    { front: { vi: "Vấn đề N+1 và cách sửa?", en: "The N+1 problem and its fix?" }, back: { vi: "Lặp N entity + truy cập navigation mỗi vòng → 1 + N query DB. Sửa: <b>eager loading</b> <code>.Include()</code> để gộp còn 1-2 query.", en: "Looping N entities + touching a navigation each time → 1 + N DB queries. Fix: <b>eager loading</b> <code>.Include()</code> to collapse into 1-2 queries." } },
    { front: { vi: "IQueryable vs IEnumerable?", en: "IQueryable vs IEnumerable?" }, back: { vi: "<b>IQueryable</b>: query dịch ra <b>SQL chạy trên DB</b> (server-side), chỉ lấy dữ liệu cần. <b>IEnumerable</b>: xử lý <b>trong bộ nhớ</b> (client-side). Cả hai deferred.", en: "<b>IQueryable</b>: query translated to <b>SQL run on the DB</b> (server-side), fetch only what's needed. <b>IEnumerable</b>: processed <b>in memory</b> (client-side). Both deferred." } },
    { front: { vi: "GC generations 0/1/2 là gì?", en: "What are GC generations 0/1/2?" }, back: { vi: "<b>Gen 0</b>: object mới, ngắn hạn — gom thường xuyên, nhanh. <b>Gen 1</b>: đệm. <b>Gen 2</b>: object sống lâu — gom full, ít nhưng tốn nhất. (\"Object chết trẻ\".)", en: "<b>Gen 0</b>: new, short-lived — collected often, fast. <b>Gen 1</b>: buffer. <b>Gen 2</b>: long-lived — full collection, rare but most expensive. (\"Objects die young.\")" } },
    { front: { vi: "Interface vs abstract class?", en: "Interface vs abstract class?" }, back: { vi: "<b>Interface</b>: hợp đồng, <b>đa</b> implement, không (truyền thống) có state. <b>Abstract class</b>: lớp cơ sở có code + field, <b>đơn</b> kế thừa, chia sẻ logic chung.", en: "<b>Interface</b>: a contract, <b>multiple</b> implementable, traditionally no state. <b>Abstract class</b>: a base with code + fields, <b>single</b> inheritance, shares common logic." } },
    { front: { vi: "Vì sao dùng using / IDisposable?", en: "Why use using / IDisposable?" }, back: { vi: "Giải phóng <b>tất định</b> tài nguyên unmanaged (file, socket, DB connection) ngay khi ra scope — GC không biết khi nào nên đóng chúng.", en: "<b>Deterministically</b> release unmanaged resources (files, sockets, DB connections) right at scope exit — the GC doesn't know when to close them." } },
  ],
  quiz: [
    { q: { vi: "DI lifetime nào tạo MỘT instance cho mỗi request HTTP?", en: "Which DI lifetime creates ONE instance per HTTP request?" },
      options: [{ vi: "Transient", en: "Transient" }, { vi: "Scoped", en: "Scoped" }, { vi: "Singleton", en: "Singleton" }, { vi: "Static", en: "Static" }], answer: 1,
      explain: { vi: "Scoped = một instance mỗi scope (mỗi request). Transient = mới mỗi lần inject; Singleton = một cái cho cả app. DbContext thường Scoped.", en: "Scoped = one instance per scope (per request). Transient = new each injection; Singleton = one for the whole app. DbContext is typically Scoped." } },
    { q: { vi: "Phát biểu nào ĐÚNG về async/await?", en: "Which statement about async/await is TRUE?" },
      options: [{ vi: "Mỗi await tạo một thread mới", en: "Each await spawns a new thread" }, { vi: "await trên I/O nhả thread về pool trong lúc chờ", en: "await on I/O frees the thread back to the pool while waiting" }, { vi: "async luôn nhanh hơn code đồng bộ", en: "async is always faster than synchronous code" }, { vi: "await chỉ dùng được trong Main", en: "await can only be used in Main" }], answer: 1,
      explain: { vi: "async/await KHÔNG tạo thread mới cho I/O — nó nhả thread về pool. Đó là lý do server async chịu tải đồng thời cao với ít thread.", en: "async/await does NOT spawn a thread for I/O — it returns the thread to the pool. That's how async servers handle high concurrency with few threads." } },
    { q: { vi: "Khác biệt cốt lõi giữa value type và reference type?", en: "Core difference between value and reference types?" },
      options: [{ vi: "Value copy theo giá trị (stack); reference copy tham chiếu (heap)", en: "Value copies by value (stack); reference copies a reference (heap)" }, { vi: "Value luôn chậm hơn", en: "Value types are always slower" }, { vi: "Reference type không thể null", en: "Reference types can't be null" }, { vi: "Chúng giống hệt nhau", en: "They are identical" }], answer: 0,
      explain: { vi: "struct/int = value type (stack, copy giá trị). class/string/array = reference type (heap, biến giữ tham chiếu → copy trỏ cùng object).", en: "struct/int = value type (stack, copied by value). class/string/array = reference type (heap, the variable holds a reference → copy points to the same object)." } },
    { q: { vi: "IQueryable<T> trong EF Core thực thi lọc ở đâu?", en: "Where does an EF Core IQueryable<T> execute filtering?" },
      options: [{ vi: "Trong bộ nhớ app sau khi lấy hết dữ liệu", en: "In app memory after loading all data" }, { vi: "Trên DB server, dịch Where ra SQL", en: "On the DB server, translating Where into SQL" }, { vi: "Trên trình duyệt", en: "In the browser" }, { vi: "Không bao giờ thực thi", en: "It never executes" }], answer: 1,
      explain: { vi: "IQueryable đẩy filter/sort xuống SQL chạy server-side, chỉ lấy dữ liệu cần. IEnumerable mới xử lý trong bộ nhớ (client-side).", en: "IQueryable pushes filter/sort into SQL run server-side, fetching only what's needed. IEnumerable processes in memory (client-side)." } },
    { q: { vi: "Cách đúng để khắc phục vấn đề N+1 trong EF Core?", en: "Correct way to fix the N+1 problem in EF Core?" },
      options: [{ vi: "Thêm nhiều thread hơn", en: "Add more threads" }, { vi: "Dùng eager loading với .Include()", en: "Use eager loading with .Include()" }, { vi: "Tắt change tracking", en: "Disable change tracking" }, { vi: "Tăng RAM server", en: "Add more server RAM" }], answer: 1,
      explain: { vi: ".Include() (eager loading) nạp quan hệ trong cùng query, gộp 1 + N round-trip thành 1-2 query. Lazy loading là nguyên nhân gây N+1.", en: ".Include() (eager loading) loads relations in the same query, collapsing 1 + N round-trips into 1-2 queries. Lazy loading is what causes N+1." } },
    { q: { vi: "Khẳng định nào ĐÚNG về .NET hiện đại (Core trở đi)?", en: "Which is TRUE about modern .NET (Core onward)?" },
      options: [{ vi: "Chỉ chạy trên Windows", en: "Runs only on Windows" }, { vi: "Cross-platform và mã nguồn mở", en: "Cross-platform and open-source" }, { vi: "Không có garbage collection", en: "Has no garbage collection" }, { vi: "Là một ngôn ngữ lập trình", en: "Is a programming language" }], answer: 1,
      explain: { vi: ".NET hợp nhất (Core → .NET 5+) cross-platform (Win/Linux/macOS), mã nguồn mở. .NET Framework cũ mới là Windows-only. .NET là nền tảng, C# là ngôn ngữ.", en: "Unified .NET (Core → .NET 5+) is cross-platform (Win/Linux/macOS), open-source. The old .NET Framework is Windows-only. .NET is a platform; C# is the language." } },
    { q: { vi: "Bẫy 'captive dependency' trong DI xảy ra khi nào?", en: "When does the DI 'captive dependency' trap occur?" },
      options: [{ vi: "Inject Singleton vào Transient", en: "Injecting a Singleton into a Transient" }, { vi: "Inject Scoped (như DbContext) vào Singleton", en: "Injecting a Scoped (like DbContext) into a Singleton" }, { vi: "Đăng ký quá nhiều Transient", en: "Registering too many Transients" }, { vi: "Dùng minimal API", en: "Using minimal APIs" }], answer: 1,
      explain: { vi: "Singleton sống suốt đời app; nếu giữ một Scoped (DbContext) thì cái đó không bao giờ được làm mới → state rò rỉ, lỗi đa luồng.", en: "A Singleton lives for the app's lifetime; if it holds a Scoped (DbContext), that one is never refreshed → leaked state, threading bugs." } },
    { q: { vi: "Tại sao cần using / IDisposable dù đã có garbage collector?", en: "Why use using / IDisposable when there's already a garbage collector?" },
      options: [{ vi: "Để tăng tốc JIT", en: "To speed up the JIT" }, { vi: "GC chỉ lo managed memory; tài nguyên unmanaged cần giải phóng tất định", en: "GC only handles managed memory; unmanaged resources need deterministic release" }, { vi: "using thay thế hoàn toàn GC", en: "using fully replaces the GC" }, { vi: "Không cần thiết, GC dọn mọi thứ", en: "It's unnecessary — the GC cleans everything" }], answer: 1,
      explain: { vi: "GC dọn bộ nhớ managed nhưng không biết khi nào đóng file/socket/DB connection (unmanaged). using/Dispose() giải phóng chúng tất định khi ra scope.", en: "The GC reclaims managed memory but doesn't know when to close files/sockets/DB connections (unmanaged). using/Dispose() releases them deterministically at scope exit." } },
  ],
});
