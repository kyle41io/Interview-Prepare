AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
DDB_TABLE=ip_progress
DDB_ENDPOINT=
SUPABASE_JWT_SECRET=
ALLOWED_ORIGINS=https://kyle41io.github.io,http://localhost:8000
PORT=3000
# Only needed for the one-time backfill script (npm run backfill), never by the API itself.
SUPABASE_DB_URL=
