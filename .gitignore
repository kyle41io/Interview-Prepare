node_modules/
.DS_Store
*.log
.playwright-mcp/
.superpowers/

# Secrets — NEVER commit. Copy .env.example → .env and fill it in locally.
.env
.env.*
!.env.example
supabase/functions/.env
docs/SETUP.md
supabase/seed/cron_gmail.local.sql

# Local AWS setup worksheet — may contain secret values, never commit
docs/superpowers/AWS-SETUP-CHECKLIST.md
